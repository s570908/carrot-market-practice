# 약속 상태 관리 시스템 구현 가이드

- Claude 3.7 Sonnet Thinking에게 다음과 같이 문의하였다.
- 약속의 상태가 다음의 네가지가 있다: 대기중 확정됨 취소됨 완료됨 . 어떤 약속 하나에 대하여 이 약속이 만들어지고 확정되고, 취소되고, 완료되는 과정들이 client의 UIUX에 주어져야 하고 백엔드의 기능도 추가되어야 할 것 같다. 이 약속에 대하여 client의 UIUX와 추가되어야할 백엔드의 기능을 예를 들어서 순차적으로 설명을 해줘. 그리고 현재의 구현체에서 추가해야할 기능과 수정사항을 자세하게 설명을 해줘. 약속의 초대자와 참가자의 client 설명을 구분해서 해줘야 한다. notification을 받았을 경우 수신자가 다뤄야 할 수신자 액션에 대한 설명이 있어야 한다. 그리고 socket io 의 기능이 구현되어 있다고 가정을 하고 자세한 설명을 해주기 바란다. 가능하면 markdown 파일로 만들어서 다운로드할 수도 있게 옵션으로 추가해줘.

## 목차

1. [약속 상태 개요](#1-약속-상태-개요)
2. [약속 생애주기 흐름](#2-약속-생애주기-흐름)
3. [상태별 UI/UX](#3-상태별-uiux)
4. [알림 시스템](#4-알림-시스템)
5. [백엔드 API 요구사항](#5-백엔드-api-요구사항)
6. [실시간 업데이트 구현](#6-실시간-업데이트-구현)
7. [현재 구현체 개선 제안](#7-현재-구현체-개선-제안)

## 1. 약속 상태 개요

약속 상태는 다음 4가지로 구분됩니다:

| 상태                   | 설명                                            | 시각적 표시   |
| ---------------------- | ----------------------------------------------- | ------------- |
| **대기중 (PENDING)**   | 약속이 생성되고 참가자들의 응답을 기다리는 상태 | 노란색/주황색 |
| **확정됨 (CONFIRMED)** | 주최자가 약속을 확정한 상태, 최종 진행 예정     | 초록색        |
| **취소됨 (CANCELLED)** | 약속이 취소된 상태, 더 이상 진행되지 않음       | 빨간색        |
| **완료됨 (COMPLETED)** | 약속이 예정대로 완료된 상태, 과거 기록          | 파란색        |

## 2. 약속 생애주기 흐름

### 전체 상태 흐름도

```
          ┌───────────┐
          │   생성    │
          └─────┬─────┘
                ▼
          ┌───────────┐       ┌───────────┐
──────────▶│  대기중   ├──────▶│  확정됨   │
│         └─────┬─────┘       └─────┬─────┘
│               │                   │
│               ▼                   ▼
│         ┌───────────┐       ┌───────────┐
└─────────┤  취소됨   │       │  완료됨   │
          └───────────┘       └───────────┘
```

### 단계별 전환 조건

1. **생성 → 대기중**

   - 주최자가 약속 생성 및 참가자 초대

2. **대기중 → 확정됨**

   - 주최자가 충분한 참가자 응답 확인 후 확정 결정
   - 필수 참가자 전원 수락 시 자동 확정 옵션

3. **대기중/확정됨 → 취소됨**

   - 주최자가 약속 취소 결정
   - 중요 참가자 불참으로 인한 취소
   - 예기치 않은 상황으로 인한 취소

4. **확정됨 → 완료됨**

   - 약속 일시가 지난 후 수동/자동 완료 처리
   - 주최자의 완료 확인

5. **취소됨 → 대기중**
   - 취소된 약속 재활성화 (일정 조정 후)

## 3. 상태별 UI/UX

### 3.1. 대기중 (PENDING)

#### 주최자 화면

1. **대시보드 표시**:

   - 응답 현황 요약 (수락/대기/거절 인원 수)
   - 수락률 그래프 시각화
   - 필수 참가자 응답 상태 강조

2. **가능한 액션**:
   - 약속 확정하기: 필요 인원 응답 시 활성화
   - 알림 다시 보내기: 미응답자에게 리마인더
   - 약속 수정: 일정/장소 변경
   - 약속 취소: 진행 어려울 경우

#### 참가자 화면

1. **초대 알림 수신**:

   - 새 약속 초대 알림 (푸시/이메일)
   - 약속 기본 정보 표시

2. **응답 인터페이스**:

   - 수락/거절 버튼
   - 일정 충돌 확인 표시
   - 메모 추가 옵션

3. **응답 후 화면**:
   - 응답 상태 표시
   - 다른 참가자 응답 현황 표시
   - 응답 변경 가능 옵션

### 3.2. 확정됨 (CONFIRMED)

#### 주최자 화면

1. **확정 표시**:

   - 확정 배지 및 상태 타임라인
   - 전체 참가자 최종 목록
   - 확정 일시/장소 정보

2. **가능한 액션**:
   - 약속 세부사항 조정 (장소 상세 등)
   - 참가자 추가 초대
   - 알림 추가 설정
   - 약속 취소 (필요시)
   - 약속 완료 처리 (약속 후)

#### 참가자 화면

1. **확정 알림 수신**:

   - 약속 확정 푸시 알림
   - 캘린더 자동 등록 제안

2. **확정 약속 화면**:
   - 확정 배지와 최종 약속 정보
   - 참가자 목록 확인
   - 지도에서 위치 확인
   - 알림 설정 옵션

### 3.3. 취소됨 (CANCELLED)

#### 주최자 화면

1. **취소 프로세스**:

   - 취소 사유 입력 대화상자
   - 참가자 알림 옵션
   - 대체 일정 제안 옵션

2. **취소 후 화면**:
   - 취소 상태 및 사유 표시
   - 새 약속 재생성 옵션
   - 참가자 알림 상태 확인

#### 참가자 화면

1. **취소 알림 수신**:

   - 약속 취소 알림
   - 취소 사유 표시
   - 대체 일정 제안 확인 (있을 경우)

2. **취소된 약속 화면**:
   - 취소 배지 및 사유 표시
   - 대체 약속 링크 (제안된 경우)
   - 캘린더에서 자동 삭제 처리

### 3.4. 완료됨 (COMPLETED)

#### 주최자 화면

1. **완료 처리 프로세스**:

   - 자동/수동 완료 옵션
   - 완료 메모 작성 폼
   - 참가자 출석 확인 옵션

2. **완료 후 화면**:
   - 완료 상태 표시
   - 참가 인원 요약
   - 피드백 수집 옵션
   - 후속 약속 생성 옵션

#### 참가자 화면

1. **완료 알림 수신**:

   - 약속 완료 알림
   - 피드백 요청 (있을 경우)

2. **완료된 약속 화면**:
   - 완료 배지 표시
   - 참석 여부 확인
   - 피드백 작성 폼 (요청된 경우)
   - 후속 약속 알림 (있을 경우)

## 4. 알림 시스템

### 알림 유형 및 발송 조건

| 알림 유형     | 발송 조건             | 수신자        | 필요 액션           |
| ------------- | --------------------- | ------------- | ------------------- |
| 약속 초대     | 약속 생성 시          | 모든 참가자   | 수락/거절 응답      |
| 응답 리마인더 | 미응답 참가자 존재 시 | 미응답 참가자 | 응답 완료           |
| 약속 확정     | 주최자가 약속 확정 시 | 모든 참가자   | 확인 및 캘린더 등록 |
| 약속 취소     | 약속 취소 시          | 모든 참가자   | 취소 사유 확인      |
| 약속 변경     | 약속 정보 변경 시     | 모든 참가자   | 변경사항 확인       |
| 예정 알림     | 약속 시간 전          | 수락한 참가자 | 약속 준비           |
| 완료 요청     | 약속 완료 시          | 주최자        | 완료 처리           |
| 피드백 요청   | 약속 완료 후          | 참석한 참가자 | 피드백 제출         |

### 알림 수신자 액션

1. **약속 초대 알림 수신 시**:

   - 약속 상세 확인하기
   - 수락/거절 응답하기
   - 응답에 메모 추가하기
   - 캘린더에 추가하기 (선택)

2. **리마인더 알림 수신 시**:

   - 응답 완료하기
   - 다시 알림 예약 (선택)
   - 약속 상세 확인하기

3. **약속 확정 알림 수신 시**:

   - 확정 내용 확인하기
   - 캘린더에 추가/업데이트하기
   - 추가 알림 설정하기 (선택)

4. **약속 취소 알림 수신 시**:

   - 취소 사유 확인하기
   - 캘린더에서 삭제하기
   - 대체 약속 확인하기 (있을 경우)

5. **약속 변경 알림 수신 시**:

   - 변경 내용 확인하기
   - 수락/거절 응답 업데이트하기
   - 캘린더 정보 업데이트하기

6. **예정 알림 수신 시**:

   - 약속 준비하기
   - 위치 확인하기
   - 길찾기 실행하기 (선택)

7. **피드백 요청 수신 시**:
   - 약속 평가하기
   - 후기 작성하기
   - 다음 약속 제안 확인하기 (있을 경우)

## 5. 백엔드 API 요구사항

### 약속 관리 API

```typescript
// 약속 상태 관리 API
interface AppointmentStatusAPI {
  // 약속 확정
  confirmAppointment(
    id: number,
    options?: {
      sendNotifications?: boolean;
      confirmationMessage?: string;
    }
  ): Promise<ApiResponse>;

  // 약속 취소
  cancelAppointment(
    id: number,
    options?: {
      cancelReason?: string;
      alternativeAppointmentId?: number;
      sendNotifications?: boolean;
    }
  ): Promise<ApiResponse>;

  // 약속 완료 처리
  completeAppointment(
    id: number,
    options?: {
      summary?: string;
      attendees?: number[];
      sendFeedbackRequest?: boolean;
    }
  ): Promise<ApiResponse>;

  // 약속 상태 변경 (일반)
  updateStatus(id: number, status: AppointmentStatus, metadata?: any): Promise<ApiResponse>;
}
```

### 참가자 응답 API

```typescript
// 참가자 응답 관리 API
interface ParticipantResponseAPI {
  // 초대 응답
  respondToInvitation(
    appointmentId: number,
    response: "confirmed" | "declined",
    options?: {
      note?: string;
      availableAlternativeDates?: string[];
    }
  ): Promise<ApiResponse>;

  // 응답 변경
  changeResponse(
    appointmentId: number,
    newResponse: "confirmed" | "declined",
    options?: {
      note?: string;
    }
  ): Promise<ApiResponse>;

  // 참가자 추가
  addParticipants(
    appointmentId: number,
    participantIds: number[],
    options?: {
      isRequired?: boolean;
    }
  ): Promise<ApiResponse>;

  // 참가자 제거
  removeParticipant(appointmentId: number, participantId: number): Promise<ApiResponse>;
}
```

### 알림 관리 API

```typescript
// 알림 관리 API
interface NotificationAPI {
  // 알림 설정 저장
  saveNotificationSettings(
    appointmentId: number,
    notifications: {
      minutesBefore: number;
      type: "PUSH" | "EMAIL" | "SMS";
      isEnabled: boolean;
    }[]
  ): Promise<ApiResponse>;

  // 알림 즉시 발송
  sendImmediateNotification(
    appointmentId: number,
    options?: {
      recipientIds?: number[]; // 특정 참가자만 대상으로 알림 발송
      message?: string;
      includeDetails?: boolean;
    }
  ): Promise<ApiResponse>;

  // 알림 읽음 처리
  markNotificationAsRead(notificationId: number): Promise<ApiResponse>;
}
```

## 6. 실시간 업데이트 구현

### Socket.IO 이벤트 구조

#### 서버측 이벤트

```javascript
// 클라이언트 연결 처리
io.on("connection", (socket) => {
  // 사용자 인증
  socket.on("authenticate", (userData) => {
    // 사용자 ID로 소켓 세션 연결
    socket.userId = userData.userId;
    socket.join(`user-${userData.userId}`);
  });

  // 약속별 룸 참여
  socket.on("joinAppointment", (appointmentId) => {
    socket.join(`appointment-${appointmentId}`);
  });
});

// 약속 상태 변경 이벤트 발송
function emitAppointmentStatusChange(appointmentId, newStatus, changedBy) {
  io.to(`appointment-${appointmentId}`).emit("appointmentStatusChanged", {
    appointmentId,
    status: newStatus,
    changedBy,
    timestamp: new Date(),
  });
}

// 참가자 응답 이벤트 발송
function emitParticipantResponse(appointmentId, participantId, response) {
  io.to(`appointment-${appointmentId}`).emit("participantResponseUpdated", {
    appointmentId,
    participantId,
    response,
    timestamp: new Date(),
  });
}
```

#### 클라이언트측 이벤트 핸들러

```javascript
// Socket.IO 연결 및 이벤트 구독
function setupSocketConnection(user) {
  const socket = io("/appointments");

  // 인증
  socket.emit("authenticate", { userId: user.id });

  // 약속 상태 변경 수신
  socket.on("appointmentStatusChanged", (data) => {
    // 상태 업데이트 및 UI 갱신
    updateAppointmentStatus(data.appointmentId, data.status);

    // 토스트 알림 표시
    toast.info(`약속이 ${getStatusText(data.status)} 상태로 변경되었습니다.`);
  });

  // 참가자 응답 수신
  socket.on("participantResponseUpdated", (data) => {
    // 참가자 응답 상태 업데이트
    updateParticipantResponse(data.appointmentId, data.participantId, data.response);

    // 진행 상황 표시 업데이트
    refreshResponseProgress(data.appointmentId);
  });

  return socket;
}
```

### 알림 수신 및 액션

#### 즉시 알림 처리

```javascript
// 새 약속 초대 알림 수신
socket.on("newAppointmentInvitation", (invitation) => {
  // 시스템 알림 표시
  showSystemNotification("새 약속 초대", {
    body: `${invitation.organizer.name}님이 '${invitation.title}' 약속에 초대했습니다.`,
    icon: "/icons/appointment.png",
    data: { appointmentId: invitation.appointmentId },
    actions: [
      { action: "view", title: "확인하기" },
      { action: "accept", title: "수락" },
      { action: "decline", title: "거절" },
    ],
  });

  // 앱 내 알림 추가
  addInAppNotification({
    type: "invitation",
    data: invitation,
    read: false,
    createdAt: new Date(),
  });
});

// 알림 액션 처리
navigator.serviceWorker.addEventListener("notificationclick", (event) => {
  const appointmentId = event.notification.data.appointmentId;

  // 액션에 따른 처리
  switch (event.action) {
    case "view":
      window.open(`/appointments/${appointmentId}`);
      break;
    case "accept":
      respondToAppointment(appointmentId, "confirmed");
      break;
    case "decline":
      respondToAppointment(appointmentId, "declined");
      break;
  }

  event.notification.close();
});
```

## 7. 현재 구현체 개선 제안

### 컴포넌트 개선사항

#### AppointmentStatusChanger 컴포넌트 개선

상태 변경 시 추가 정보를 수집하는 기능을 추가하고, 취소 사유나 완료 메모 등을 입력받을 수 있는 다이얼로그를 구현합니다.

#### 참가자 응답 관리 컴포넌트 추가

주최자가 참가자의 응답을 관리하고, 필요한 액션(리마인더 발송, 참가자 추가/제거)을 취할 수 있는 컴포넌트가 필요합니다.

#### 실시간 알림 통합

Socket.IO를 활용한 실시간 알림 시스템을 구현하여 상태 변경, 참가자 응답 등의 이벤트를 실시간으로 전달합니다.

#### 약속 상태 흐름 관리 개선

약속의 상태 전환과 추가 정보 처리를 위한 커스텀 훅을 개발하여 상태 관리를 일관되게 처리합니다.

### 알림 서비스 구현

실시간 알림, 예약된 알림 등을 통합 관리할 수 있는 서비스 레이어를 구현합니다.

### 백엔드 API 확장

기존 API를 확장하여 상태 관리, 참가자 응답, 알림 발송 등을 더 세밀하게 관리할 수 있는 엔드포인트를 추가합니다.
