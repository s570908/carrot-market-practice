## 약속 생성 및 알람 설정 전체 워크플로우 상세 분석

### 1. 약속 생성 페이지에서 시작 (pages/appointment/create.tsx)

- 사용자가 약속 생성 폼을 작성하고 제출하면 `createMeetup` mutation이 실행됩니다.
- `createMeetup`은 내부적으로 `writeChatMeetup` API를 호출하여 약속(appointment)과 관련 메시지를 생성합니다.

```tsx
const { mutate: createMeetup, status } = useMutation<
  ChatMeetupResponse, 
  Error, 
  ChatMeetupParams
>({
  mutationFn: writeChatMeetup,
  onSuccess: async (responseData) => {
    // 1. 약속 생성 시스템 메시지 추가
    await writeSystemMessage({
      chatRoomId: chatRoomId,
      message: SYSTEM_MESSAGES.APPOINTMENT_CREATED(responseData.chatMeetup.appointmentTime),
      userId: user?.id,
    });

    // 2. 알림이 설정된 경우, 알림 메시지 추가
    if (responseData.chatMeetup?.alarmTime) {
      const appointmentAlertInfo = SYSTEM_MESSAGES.APPOINTMENT_ALERT(
        responseData.chatMeetup.alarmTime, 
        responseData.chatMeetup.id
      );
      await writeSystemMessage({
        chatRoomId: chatRoomId,
        message: appointmentAlertInfo.message,
        userId: user?.id,
        meta: appointmentAlertInfo.meta
      });
    }
    
    // 3. 알람 설정 API 호출 (최초 생성)
    if (responseData.chatMeetup?.alarmTime && responseData.message?.id) {
      const triggerAt = calculateTriggerTime(
        new Date(responseData.chatMeetup.appointmentTime),
        responseData.chatMeetup.alarmTime
      );
      
      await writeAlarmSettings({
        chatId: chatRoomId,
        messageId: responseData.message.id,
        alarmTime: responseData.chatMeetup.alarmTime,
        triggerAt: triggerAt.toISOString(),
        disableAlarm: false
      });
    }
  }
});
```

#### 요약
- 약속 생성 → 시스템 메시지 추가 → (알림 설정 시) 알림 메시지 추가 → 알람 스케줄 등록

---

### ✅ 장점 및 적절성

- **트랜잭션 처리**: 약속 메시지와 약속(chatMeetup) 생성이 트랜잭션으로 안전하게 처리됩니다.
- **상태 동기화**: 알람 스케줄러(activeJobs)와 DB(alarmSetting) 상태가 일치하도록 관리됩니다.
- **알람 변경/취소**: 기존 알람이 있으면 항상 취소 후 새로 등록하여 중복/충돌을 방지합니다.
- **실시간 반영**: 소켓, 시스템 메시지, 푸시 알림 등 다양한 채널로 즉시 반영됩니다.
- **에러 처리**: 각 단계별로 적절한 에러 처리와 예외 상황 대응이 구현되어 있습니다.

---

### ⚠️ 개선/주의사항

- **타입 안정성**: 일부 코드에서 `(alarm as any).alarmTime` 등 타입 캐스팅이 사용되고 있습니다.  
  → alarm 객체의 타입 정의를 명확히 하여 타입 안정성을 높이는 것이 좋습니다.
- **동시성**: 여러 사용자가 동시에 알람을 변경할 때 race condition이 발생할 수 있습니다.  
  → 트랜잭션 범위 확대 또는 락(lock) 처리 검토가 필요할 수 있습니다.
- **web-push 알림**: 실제 web-push 연동이 완전히 구현되어 있는지, 실패 시 재시도/로깅이 필요한지 점검 필요합니다.
- **시간대(Timezone)**: appointmentTime, triggerAt 등 날짜/시간 처리 시 서버와 클라이언트의 시간대가 일치하는지 확인이 필요합니다.
- **에러 메시지**: 사용자에게 노출되는 에러 메시지는 보안상/UX상 적절한지 검토 필요합니다.

---

### 📝 결론

- **현재 구조는 실무적으로도 충분히 견고하며, 주요 기능이 모두 잘 동작할 것으로 판단됩니다.**
- **위 개선사항은 코드 품질과 안정성을 한 단계 더 높이기 위한 권장사항입니다.**

---

### 2. 약속/알람 DB 생성 및 메시지 트랜잭션 (pages/api/chat-meetups/index.ts)

- `writeChatMeetup` API는 약속 메시지와 약속(chatMeetup) 레코드를 트랜잭션으로 생성합니다.
- 생성 후, 소켓을 통해 채팅방에 실시간 메시지를 전송합니다.

```ts
// ...existing code...
const [message, chatMeetup] = await client.$transaction(async (prisma) => {
  // 1. sellerChat 메시지 생성
  // 2. chatMeetup(약속) 생성 및 메시지 연결
  // ...existing code...
  return [createdMessage, createdChatMeetup];
});

// 3. 소켓 이벤트 전송 (채팅방에 실시간 반영)
// ...existing code...
```

---

### 3. 알람 설정 API 호출 및 처리 (pages/api/chat/[id]/alarm-settings.ts)

- 약속 메시지와 연결된 약속(chatMeetup) 정보를 조회합니다.
- 약속 시간이 이미 지났으면 에러 반환.
- 기존 알람이 있으면 취소(`cancelExistingAlarm`).
- 새 알람 설정을 DB에 생성하고, 스케줄러에 등록(`scheduleAlarmById`).

```ts
// ...existing code...
const message = await client.sellerChat.findUnique({
  where: { id: +messageId },
  include: { chatMeetup: true }
});

// 약속 시간이 이미 지났는지 확인
const appointmentTime = new Date(message.chatMeetup.appointmentTime);
const now = new Date();

if (appointmentTime < now) {
  return res.status(400).json({ 
    ok: false, 
    error: "past appointment: 이미 지난 약속에는 알림을 설정할 수 없습니다." 
  });
}

// 기존 알람 설정이 있는지 확인
const existingAlarm = await client.alarmSetting.findFirst({
  where: {
    userId: user.id,
    chatMeetupId: message.chatMeetup.id,
    status: { in: [AlarmStatus.SCHEDULED] }
  }
});

if (existingAlarm) {
  // 기존 알람 취소 및 새 알람 설정
  await cancelExistingAlarm(existingAlarm.id);
}

// 새로운 알람 설정 생성
const alarmSetting = await client.alarmSetting.create({
  data: {
    userId: user.id,
    chatRoomId: +id,
    messageId: +messageId,
    chatMeetupId: message.chatMeetup.id,
    alarmTime,
    triggerAt: triggerAtDate,
    status: AlarmStatus.SCHEDULED,
  }
});

// 스케줄러에 등록
const baseUrl = process.env.NEXT_PUBLIC_API_URL || `https://${req.headers.host}`;
await scheduleAlarmById(alarmSetting.id, baseUrl);
```

---

### 4. 알람 스케줄러 등록 및 관리 (libs/server/alarmScheduler.ts)

- `scheduleAlarmById`은 알람 정보를 받아 스케줄러에 등록합니다.
- 기존에 등록된 작업이 있으면 취소 후 새로 등록.
- 알람 트리거 시, 상태를 SENT로 변경하고 `/api/alarm/trigger` 호출.
- `cancelExistingAlarm`은 메모리와 DB에서 알람을 취소 처리.

```ts
export async function scheduleAlarmById(alarmId: number, baseUrl: string) {
  const alarm = await client.alarmSetting.findUnique({
    where: { id: alarmId },
    include: { chatMeetup: true },
  });
  
  if (
    alarm &&
    (alarm as any).alarmTime &&
    alarm.status === AlarmStatus.SCHEDULED &&
    alarm.triggerAt > new Date()
  ) {
    scheduleAlarm(alarm, baseUrl);
    return true;
  }
  return false;
}

export function scheduleAlarm(alarm: any, baseUrl: string) {
  if (!alarm.alarmTime) return;
  
  // 기존 작업이 있다면 취소 (중요: 알람 변경 시 기존 스케줄 제거)
  if (activeJobs.has(alarm.id)) {
    activeJobs.get(alarm.id).cancel();
  }
  
  const job = schedule.scheduleJob(alarm.triggerAt, async function() {
    try {
      console.log(`Triggering alarm ID: ${alarm.id} at ${new Date().toISOString()}`);
      
      // AlarmSetting 업데이트: status를 SENT로 변경
      const updatedAlarm = await client.alarmSetting.update({
        where: { id: alarm.id },
        data: { status: AlarmStatus.SENT },
        include: {
          chatMeetup: {
            include: {
              message: {
                include: {
                  chatRoom: true
                }
              }
            }
          },
          user: true,
        },
      });

      // 핵심: 알람 트리거 API 호출
      const response = await axios.post(`${baseUrl}/api/alarm/trigger`, {
        alarmId: alarm.id
      });
      
      console.log(`Alarm triggered successfully: ${JSON.stringify(response.data)}`);
      
      // 작업 완료 후 Map에서 제거
      activeJobs.delete(alarm.id);
    } catch (error) {
      console.error(`Error triggering alarm ID ${alarm.id}:`, error);
    }
  });
  
  // 활성 작업 Map에 추가
  activeJobs.set(alarm.id, job);
  
  const formattedTime = dayjs(alarm.triggerAt).format('YYYY-MM-DD HH:mm:ss');
  console.log(`Scheduled alarm ID ${alarm.id} for ${formattedTime}`);
}

// 기존 알람 취소 함수 (새로 추가)
export async function cancelExistingAlarm(alarmId: number) {
  try {
    // DB 상태 확인 후 메모리에서 제거
    const alarm = await client.alarmSetting.findUnique({
      where: { id: alarmId },
      select: { id: true, status: true }
    });

    // 이미 취소되었거나 전송된 알람은 스킵
    if (!alarm || alarm.status !== AlarmStatus.SCHEDULED) {
      console.log(`Alarm ID ${alarmId} is not in SCHEDULED status, skipping cancellation`);
      return true;
    }

    // 스케줄된 작업 취소
    if (activeJobs.has(alarmId)) {
      activeJobs.get(alarmId).cancel();
      activeJobs.delete(alarmId);
      console.log(`Cancelled scheduled job for alarm ID: ${alarmId}`);
    }
    
    // DB에서 알람 상태 업데이트
    await client.alarmSetting.update({
      where: { id: alarmId },
      data: { status: AlarmStatus.CANCELLED }
    });
    
    return true;
  } catch (error) {
    console.error(`Error cancelling alarm ID ${alarmId}:`, error);
    return false;
  }
}

// 서버 시작 시 기존 스케줄 복구 로직
export async function initializeAlarmScheduler(baseUrl: string) {
  console.log('Initializing alarm scheduler...');
  
  try {
    // 서버 재시작 시 기존의 SCHEDULED 상태 알람들을 다시 스케줄링
    const scheduledAlarms = await client.alarmSetting.findMany({
      where: {
        status: AlarmStatus.SCHEDULED,
        triggerAt: { gt: new Date() } // 미래의 알람만
      },
      include: { chatMeetup: true }
    });

    console.log(`Found ${scheduledAlarms.length} alarms to reschedule`);
    
    for (const alarm of scheduledAlarms) {
      if (alarm.alarmTime) {
        scheduleAlarm(alarm, baseUrl);
      }
    }
    
    return scheduledAlarms.length;
  } catch (error) {
    console.error('Error initializing alarm scheduler:', error);
    return 0;
  }
}
```

---

### 5. 알람 트리거 API (pages/api/alarm/trigger.ts)

- 스케줄러에서 호출되어, 실제로 푸시 알림/소켓 메시지/시스템 메시지를 발송합니다.

```ts
export default async function handler(
  req: NextApiRequest,
  res: NextApiResponse
) {
  if (req.method !== "POST") {
    return res.status(405).json({ ok: false, error: "Method not allowed" });
  }

  const { alarmId } = req.body;

  try {
    // 알람 정보 조회 (개인 알람이므로 설정한 사용자 정보만 필요)
    const alarm = await client.alarmSetting.findUnique({
      where: { id: alarmId },
      include: {
        user: true,
        chatMeetup: {
          include: {
            message: true
          }
        }
      }
    });

    if (!alarm) {
      return res.status(404).json({ ok: false, error: "Alarm not found" });
    }

    // 1. 개인 푸시 알림 발송 (알람 설정한 사용자에게만)
    const pushNotificationData = {
      title: "약속 알림",
      body: `${alarm.chatMeetup.place}에서의 약속이 ${alarm.alarmTime} 남았습니다.`,
      chatRoomId: alarm.chatRoomId,
      appointmentId: alarm.chatMeetupId,
    };

    // 알람을 설정한 사용자에게만 푸시 알림 발송
    if (alarm.user.fcmToken) {
      await sendPushNotification(alarm.user.fcmToken, pushNotificationData);
    }

    // 2. 개인 소켓 메시지 발송 (알람 설정한 사용자에게만)
    if (res?.socket?.server?.io) {
      const socketData = {
        type: "alarm_notification",
        alarmId: alarm.id,
        chatRoomId: alarm.chatRoomId,
        message: pushNotificationData.body,
        appointmentTime: alarm.chatMeetup.appointmentTime,
        place: alarm.chatMeetup.place,
      };
      
      // 알람 설정한 사용자에게만 개인 알림
      res.socket.server.io
        .of(`ws-${process.env.WORKSPACE}`)
        .to(`user-${alarm.userId}`)
        .emit("alarm", socketData);
    }

    // 3. 개인 알림이므로 시스템 메시지는 생성하지 않음
    // (채팅방 전체에 알릴 필요가 없는 개인 알람)

    return res.status(200).json({ 
      ok: true, 
      message: "Personal alarm triggered successfully",
      notificationsSent: 1 // 개인 알람이므로 1명
    });

  } catch (error) {
    console.error("Error triggering alarm:", error);
    return res.status(500).json({ 
      ok: false, 
      error: "Internal server error" 
    });
  }
}
```

---

### 6. 채팅방에서 알람 설정 변경 (pages/chats/[id].tsx)

- 사용자가 채팅방에서 알람 버튼 클릭 → 알람 시간 선택 → 기존 알람이 있으면 취소 후 새로 등록.
- 알람 비활성화도 지원.

```tsx
// 알람 버튼 클릭 핸들러
const handleAlarmButtonClick = (messageId: number) => {
  const systemMessage = data?.sellerChat?.find(msg => msg.id === messageId);
  
  if (!systemMessage) {
    alert('메시지 정보를 찾을 수 없습니다.');
    return;
  }

  const metaObj = getMetaData(systemMessage.meta);
  const chatMeetupId = metaObj?.chatMeetupId;
  
  const appointmentMessage = data?.sellerChat?.find(msg => 
    msg.chatMeetup && msg.chatMeetup.id === chatMeetupId
  );
  
  if (appointmentMessage?.chatMeetup) {
    const appointmentTime = new Date(appointmentMessage.chatMeetup.appointmentTime);
    const now = new Date();
    const isPast = appointmentTime.getTime() < now.getTime();
    
    if (isPast) {
      alert('이미 지난 약속에는 알림을 설정할 수 없습니다.');
      return;
    }
    
    setCurrentMessageId(appointmentMessage.id);
    setCurrentChatMeetupId(chatMeetupId);
    setAlarmSheetOpen(true);
  }
};

// 알람 시간 선택 핸들러 (변경 처리 포함)
const handleAlarmTimeSelected = async (timeOption: string) => {
  if (!currentMessageId || !currentChatMeetupId) return;
  
  try {
    // 현재 약속 정보 가져오기
    const appointmentMessage = data?.sellerChat?.find(msg => 
      msg.chatMeetup && msg.chatMeetup.id === currentChatMeetupId
    );
    
    if (!appointmentMessage?.chatMeetup) {
      alert('약속 정보를 찾을 수 없습니다.');
      return;
    }
    
    const appointmentTime = new Date(appointmentMessage.chatMeetup.appointmentTime);
    const triggerAt = calculateTriggerTime(appointmentTime, timeOption);
    
    // 기존 알람이 있는지 확인 후 설정
    const existingAlarmMessage = data?.sellerChat?.find(msg => 
      msg.meta && getMetaData(msg.meta)?.chatMeetupId === currentChatMeetupId &&
      getMetaData(msg.meta)?.type === 'appointment_alert'
    );
    
    if (existingAlarmMessage) {
      // 기존 알람 설정 변경
      await updateAlarmSettings({
        chatId: id,
        messageId: currentMessageId,
        alarmTime: timeOption,
        triggerAt: triggerAt.toISOString(),
        disableAlarm: false
      });
      
      // 기존 알람 메시지 업데이트
      await updateSystemMessage({
        messageId: existingAlarmMessage.id,
        message: `🔔 알림이 약속 ${timeOption} 전으로 변경되었습니다.`,
        meta: JSON.stringify({
          type: 'appointment_alert',
          chatMeetupId: currentChatMeetupId,
          alarmTime: timeOption
        })
      });
    } else {
      // 새로운 알람 설정
      await writeAlarmSettings({
        chatId: id,
        messageId: currentMessageId,
        alarmTime: timeOption,
        triggerAt: triggerAt.toISOString(),
        disableAlarm: false
      });
      
      // 새로운 알람 메시지 추가
      const appointmentAlertInfo = SYSTEM_MESSAGES.APPOINTMENT_ALERT(
        timeOption, 
        currentChatMeetupId
      );
      await writeSystemMessage({
        chatRoomId: id,
        message: appointmentAlertInfo.message,
        userId: user?.id,
        meta: appointmentAlertInfo.meta
      });
    }
    
    setAlarmSheetOpen(false);
    
    // 데이터 새로고침
    await refetch();
    
  } catch (error) {
    console.error("알림 설정 중 오류 발생:", error);
    alert("알림 설정에 실패했습니다.");
  }
};

// 알람 비활성화 핸들러
const handleDisableAlarm = async () => {
  if (!currentMessageId || !currentChatMeetupId) return;
  
  try {
    // 알람 비활성화
    await updateAlarmSettings({
      chatId: id,
      messageId: currentMessageId,
      alarmTime: null,
      triggerAt: null,
      disableAlarm: true
    });
    
    // 알람 비활성화 메시지 추가
    await writeSystemMessage({
      chatRoomId: id,
      message: '🔕 알림이 비활성화되었습니다.',
      userId: user?.id,
      meta: JSON.stringify({
        type: 'appointment_alert_disabled',
        chatMeetupId: currentChatMeetupId
      })
    });
    
    setAlarmSheetOpen(false);
    await refetch();
    
  } catch (error) {
    console.error("알림 비활성화 중 오류 발생:", error);
    alert("알림 비활성화에 실패했습니다.");
  }
};
```

---

### 7. 알람 설정/변경 API (apiLibs/chats.ts, pages/api/chat/[id]/alarm-settings.ts)

- 클라이언트에서 알람 설정/변경/비활성화 요청을 PUT/POST로 보냄.
- 서버는 기존 알람 취소 후 새 알람 생성, 또는 비활성화 처리.

```ts
export interface UpdateAlarmSettingsParams {
  chatId: number;
  messageId: number;
  alarmTime: string | null;
  triggerAt: string | null;
  disableAlarm: boolean;
}

export async function updateAlarmSettings(params: UpdateAlarmSettingsParams) {
  const response = await aclient.put<AlarmSettingsResponse>(
    `/api/chat/${params.chatId}/alarm-settings`,
    params
  );
  return response.data;
}

export async function updateSystemMessage(params: {
  messageId: number;
  message: string;
  meta?: string;
}) {
  const response = await aclient.put(`/api/messages/${params.messageId}`, params);
  return response.data;
}

export default async function handler(
  req: NextApiRequest,
  res: NextApiResponse
) {
  if (req.method === "POST") {
    // 기존 POST 로직...
  } 
  else if (req.method === "PUT") {
    // 알람 설정 업데이트 로직
    const { id } = req.query;
    const { messageId, alarmTime, triggerAt, disableAlarm } = req.body;
    const { user } = req.session;

    if (!user?.id) {
      return res.status(401).json({ ok: false, error: "Unauthorized" });
    }

    try {
      // 메시지와 관련된 약속 정보 가져오기
      const message = await client.sellerChat.findUnique({
        where: { id: +messageId },
        include: { chatMeetup: true }
      });

      if (!message?.chatMeetup) {
        return res.status(404).json({ 
          ok: false, 
          error: "Appointment not found" 
        });
      }

      // 트랜잭션으로 동시성 제어 - 기존 알람 취소와 새 알람 생성을 원자적으로 처리
      const result = await client.$transaction(async (tx) => {
        // 기존 알람 설정 찾기 (락과 함께)
        const existingAlarm = await tx.alarmSetting.findFirst({
          where: {
            userId: user.id,
            chatMeetupId: message.chatMeetup.id,
            status: { in: [AlarmStatus.SCHEDULED] }
          },
          // PostgreSQL의 경우 FOR UPDATE 사용
          // SQLite는 지원하지 않으므로 환경에 따라 조건부 적용
        });

        // 기존 알람이 있으면 취소
        if (existingAlarm) {
          await tx.alarmSetting.update({
            where: { id: existingAlarm.id },
            data: { status: AlarmStatus.CANCELLED }
          });
          
          // 메모리에서도 즉시 제거 (스케줄러 취소는 트랜잭션 외부에서)
          return { 
            cancelledAlarmId: existingAlarm.id, 
            existingAlarm,
            newAlarm: null 
          };
        }

        // 알람 비활성화가 아닌 경우에만 새 알람 생성
        if (!disableAlarm && alarmTime && triggerAt) {
          const triggerAtDate = new Date(triggerAt);
          const newAlarm = await tx.alarmSetting.create({
            data: {
              userId: user.id,
              chatRoomId: +id,
              messageId: +messageId,
              chatMeetupId: message.chatMeetup.id,
              alarmTime,
              triggerAt: triggerAtDate,
              status: AlarmStatus.SCHEDULED,
            }
          });
          
          return { 
            cancelledAlarmId: existingAlarm?.id || null, 
            existingAlarm,
            newAlarm 
          };
        }

        return { 
          cancelledAlarmId: existingAlarm?.id || null, 
          existingAlarm,
          newAlarm: null 
        };
      });

      // 트랜잭션 외부에서 스케줄러 관리
      // 1. 기존 스케줄 취소
      if (result.cancelledAlarmId) {
        await cancelExistingAlarm(result.cancelledAlarmId);
      }

      // 2. 새 스케줄 등록
      if (result.newAlarm) {
        const baseUrl = process.env.NEXT_PUBLIC_API_URL || `https://${req.headers.host}`;
        await scheduleAlarmById(result.newAlarm.id, baseUrl);
      }

      return res.status(200).json({
        ok: true,
        alarmSetting: result.newAlarm,
        message: disableAlarm ? "Alarm disabled successfully" : "Alarm updated successfully",
        cancelled: !!result.cancelledAlarmId
      });

    } catch (error) {
      console.error("Error updating alarm settings:", error);
      return res.status(500).json({ 
        ok: false, 
        error: "Internal server error" 
      });
    }
  }
  else {
    return res.status(405).json({ ok: false, error: "Method not allowed" });
  }
}
```

---

## 전체 워크플로우 요약

1. **약속 생성**: createMeetup → writeChatMeetup → 메시지/약속 트랜잭션 생성 → 소켓 전송
2. **알람 설정**: writeAlarmSettings → alarmSetting DB에 알람 정보 저장 → scheduleAlarmById 등록
3. **알람 트리거**: 스케줄러에서 시간 도래 시 alarm/trigger 호출 → 푸시/소켓/시스템 메시지 발송
4. **알람 변경/비활성화**: 채팅방에서 알람 버튼 → updateAlarmSettings(기존 취소 후 새로 등록/비활성화)
5. **상태 동기화**: activeJobs(메모리)와 alarmSetting(DB) 상태 일치 유지

---

## 채팅방 알람 변경 워크플로우

1. **사용자 액션**: 채팅방 알람 버튼 클릭
2. **기존 알람 확인 및 취소**: cancelExistingAlarm 호출
3. **새 알람 등록**: updateAlarmSettings로 새 알람 생성 및 스케줄러 등록
4. **UI/메시지 반영**: 시스템 메시지/알람 메시지 갱신

---

## 결론

- 약속 생성부터 알람 설정, 변경, 취소, 트리거까지 모든 단계가 트랜잭션과 상태 동기화로 안전하게 처리됩니다.
- 실시간 소켓, 푸시 알림, 시스템 메시지로 사용자 경험이 강화됩니다.
- 메모리(activeJobs)와 DB(alarmSetting) 상태가 항상 일치하도록 관리됩니다.

---

## createMeetup 이후 전체 워크플로우 추적 및 점검 (web-push, node-schedule 기준)

### 1. 약속 메시지 생성 (pages/appointment/create.tsx)
- 사용자가 폼을 제출하면 `createMeetup` mutation이 실행됨.
- 내부적으로 `writeChatMeetup` API 호출 → 약속 메시지(sellerChat)와 약속(chatMeetup) 트랜잭션 생성.

### 2. 시스템 메시지 및 알림 메시지 추가
- 약속 생성 성공 시, 시스템 메시지(`APPOINTMENT_CREATED`)를 채팅방에 추가.
- 알림(alarmTime)이 설정된 경우, 알림 메시지(`APPOINTMENT_ALERT`)도 추가.

### 3. 알람 설정 API 호출
-alarmTime이 있으면 `writeAlarmSettings` 호출 → alarmSetting DB에 알람 정보 저장.
- triggerAt(알림 트리거 시각) 계산 후 저장.

### 4. 알람 스케줄러 등록 (libs/server/alarmScheduler.ts, node-schedule)
- `scheduleAlarmById` 호출 → alarmSetting DB에서 알람 정보 조회.
- 조건(alarm.alarmTime, status, triggerAt) 만족 시 `scheduleAlarm`으로 node-schedule에 등록.
- 기존 스케줄이 있으면 취소 후 새로 등록.

### 5. 알람 변경/비활성화 (pages/chats/[id].tsx)
- 사용자가 채팅방에서 알람 버튼 클릭 → 알람 시간 선택/변경/비활성화.
- 기존 알람이 있으면 `cancelExistingAlarm`으로 취소 후, 새 알람 설정 시 `updateAlarmSettings`로 갱신.
- 알람 메시지/시스템 메시지 UI도 즉시 반영.

### 6. 알람 트리거 (정해진 시간에 실행, node-schedule)
- 스케줄러가 triggerAt에 도달하면, 알람 트리거 함수 실행.
- alarmSetting 상태를 SENT로 변경, `/api/alarm/trigger` API 호출.

### 7. 알림 전송 (pages/api/alarm/trigger.ts, web-push)
- 알람 정보와 채팅방 참여자 조회.
- web-push로 알림 전송, 소켓 메시지, 시스템 메시지 생성.

---

## 순서 및 연계 점검

- [✔] 약속 메시지/알람 메시지/알람 설정/스케줄 등록/알림 트리거/알림 전송이 순차적으로 잘 연결되어 있음.
- [✔] 알람 변경/취소 시 기존 스케줄과 DB 상태가 일관되게 관리됨.
- [✔] 실시간 UI 반영 및 에러 처리도 적절함.
- [✔] web-push와 node-schedule이 실제로 사용되고 있음.

---

## 개선점 및 잠재적 오류

- **타입 안정성**: alarm 객체 타입을 명확히 정의해 타입 캐스팅 제거 필요(이미 개선 제안 적용 가능).
- **동시성**: 여러 사용자가 동시에 알람을 변경할 때 race condition 가능성 있음. 트랜잭션/락 검토 필요.
- **시간대**: 서버/클라이언트 시간대 불일치로 인한 오동작 가능성. UTC 일관성 유지 필요.
- **web-push 실패 처리**: 알림 전송 실패 시 재시도/로깅 강화 필요.
- **불필요한 메시지 중복**: 알람 메시지/시스템 메시지 중복 생성 방지 로직 필요할 수 있음.

---

## 결론

- 전체 워크플로우가 논리적으로 순서에 맞게 잘 연결되어 있음.
- 실시간성, 상태 동기화, 에러 처리 등 실무적으로도 견고함.
- 위 개선점(타입, 동시성, 시간대, 실패 처리 등)만 보완하면 더욱 안정적임.

## 🚨 중요한 개선점 및 잠재적 오류 발견

### 1. **에러 처리 부족 - 치명적 문제**
현재 `pages/appointment/create.tsx`에서 시스템 메시지 추가나 알람 설정이 실패해도 사용자가 알 수 없음:
```tsx
// 현재 문제점: 에러가 발생해도 silent fail
onSuccess: async (responseData) => {
  await writeSystemMessage({...}); // 실패 시 사용자 모름
  await writeAlarmSettings({...}); // 실패 시 사용자 모름
}
```

### 2. **시간대 불일치 - 심각한 문제**
클라이언트와 서버 간 시간대 차이로 인한 알람 오동작 가능성:
- 클라이언트에서 계산한 triggerAt이 서버 시간대와 다를 수 있음
- UTC 기준 일관성 없음

### 3. **Race Condition - 동시성 문제**
여러 사용자가 동시에 같은 약속의 알람을 변경할 때:
- 기존 알람 취소와 새 알람 생성 사이에 gap 존재
- 트랜잭션 범위가 충분하지 않음

### 4. **web-push 실패 처리 부족**
`pages/api/alarm/trigger.ts`에서:
- 푸시 알림 실패 시 재시도 로직 없음
- 실패한 사용자 추적 불가
- 부분 실패 시 전체 실패로 처리

### 5. **메모리 누수 가능성**
`libs/server/alarmScheduler.ts`에서:
- activeJobs Map에서 완료된 작업이 제대로 정리되지 않을 수 있음
- 서버 재시작 시 기존 스케줄 복구 로직 부족

### 6. **메시지 중복 생성 위험**
알람 메시지와 시스템 메시지가 중복 생성될 가능성:
- 네트워크 재시도로 인한 중복 요청
- 클라이언트 side effect 중복 실행

---

## 📋 개선된 워크플로우 제안

### A. 에러 처리 강화
```tsx
onSuccess: async (responseData) => {
  try {
    // 시스템 메시지 추가
    await writeSystemMessage({...});
    
    // 알람 설정 (실패해도 약속은 유지)
    if (responseData.chatMeetup?.alarmTime) {
      try {
        await writeAlarmSettings({...});
        await writeSystemMessage({...}); // 알람 메시지
      } catch (alarmError) {
        console.error("알람 설정 실패:", alarmError);
        toast.error("알람 설정에 실패했습니다. 채팅방에서 다시 설정해주세요.");
      }
    }
  } catch (error) {
    console.error("약속 후처리 실패:", error);
    toast.error("약속은 생성되었으나 일부 기능에 문제가 발생했습니다.");
  }
}
```

### B. 시간대 일관성 보장
```tsx
// UTC 기준으로 시간 계산 및 저장
const utcAppointmentTime = new Date(responseData.chatMeetup.appointmentTime);
const utcTriggerAt = calculateTriggerTime(utcAppointmentTime,alarmTime);
await writeAlarmSettings({
  // ...other params...
  triggerAt: utcTriggerAt.toISOString(), // UTC 문자열로 저장
});
```

### C. 트랜잭션 범위 확대 - 실제 구현 예시
```ts
// pages/api/chat/[id]/alarm-settings.ts에서 동시성 제어 강화
export default async function handler(req: NextApiRequest, res: NextApiResponse) {
  if (req.method === "POST" || req.method === "PUT") {
    const { id } = req.query;
    const { messageId, alarmTime, triggerAt, disableAlarm } = req.body;
    const { user } = req.session;

    if (!user?.id) {
      return res.status(401).json({ ok: false, error: "Unauthorized" });
    }

    try {
      // 메시지와 관련된 약속 정보 가져오기
      const message = await client.sellerChat.findUnique({
        where: { id: +messageId },
        include: { chatMeetup: true }
      });

      if (!message?.chatMeetup) {
        return res.status(404).json({ 
          ok: false, 
          error: "Appointment not found" 
        });
      }

      // 트랜잭션으로 동시성 제어 - 기존 알람 취소와 새 알람 생성을 원자적으로 처리
      const result = await client.$transaction(async (tx) => {
        // 기존 알람 설정 찾기 (락과 함께)
        const existingAlarm = await tx.alarmSetting.findFirst({
          where: {
            userId: user.id,
            chatMeetupId: message.chatMeetup.id,
            status: { in: [AlarmStatus.SCHEDULED] }
          },
          // PostgreSQL의 경우 FOR UPDATE 사용
          // SQLite는 지원하지 않으므로 환경에 따라 조건부 적용
        });

        // 기존 알람이 있으면 취소
        if (existingAlarm) {
          await tx.alarmSetting.update({
            where: { id: existingAlarm.id },
            data: { status: AlarmStatus.CANCELLED }
          });
          
          // 메모리에서도 즉시 제거 (스케줄러 취소는 트랜잭션 외부에서)
          return { 
            cancelledAlarmId: existingAlarm.id, 
            existingAlarm,
            newAlarm: null 
          };
        }

        // 알람 비활성화가 아닌 경우에만 새 알람 생성
        if (!disableAlarm && alarmTime && triggerAt) {
          const triggerAtDate = new Date(triggerAt);
          const newAlarm = await tx.alarmSetting.create({
            data: {
              userId: user.id,
              chatRoomId: +id,
              messageId: +messageId,
              chatMeetupId: message.chatMeetup.id,
              alarmTime,
              triggerAt: triggerAtDate,
              status: AlarmStatus.SCHEDULED,
            }
          });
          
          return { 
            cancelledAlarmId: existingAlarm?.id || null, 
            existingAlarm,
            newAlarm 
          };
        }

        return { 
          cancelledAlarmId: existingAlarm?.id || null, 
          existingAlarm,
          newAlarm: null 
        };
      });

      // 트랜잭션 외부에서 스케줄러 관리
      // 1. 기존 스케줄 취소
      if (result.cancelledAlarmId) {
        await cancelExistingAlarm(result.cancelledAlarmId);
      }

      // 2. 새 스케줄 등록
      if (result.newAlarm) {
        const baseUrl = process.env.NEXT_PUBLIC_API_URL || `https://${req.headers.host}`;
        await scheduleAlarmById(result.newAlarm.id, baseUrl);
      }

      return res.status(200).json({
        ok: true,
        alarmSetting: result.newAlarm,
        message: disableAlarm ? "Alarm disabled successfully" : "Alarm updated successfully",
        cancelled: !!result.cancelledAlarmId
      });

    } catch (error) {
      console.error("Error updating alarm settings:", error);
      return res.status(500).json({ 
        ok: false, 
        error: "Internal server error" 
      });
    }
  }
  else {
    return res.status(405).json({ ok: false, error: "Method not allowed" });
  }
}
```

### D. web-push 재시도 로직 - 개인 알람 버전
```ts
// pages/api/alarm/trigger.ts
let pushNotificationSuccess = false;

// 개인 알람이므로 1명에게만 재시도
if (alarm.user.pushSubscription) {
  try {
    await retryWebPush(alarm.user.pushSubscription, notificationData, 3);
    pushNotificationSuccess = true;
  } catch (error) {
    console.error(`Push notification failed for user ${alarm.userId}:`, error);
    
    // 개인 알람 실패 로깅
    await client.pushNotificationLog.create({
      data: {
        alarmId,
        failedUserId: alarm.userId,
        error: "Personal alarm push notification delivery failed"
      }
    });
  }
}
```

이러한 트랜잭션 범위 확대로 다음과 같은 이점을 얻을 수 있습니다:

1. **원자성 보장**: 기존 알람 취소와 새 알람 생성이 모두 성공하거나 모두 실패
2. **동시성 제어**: 여러 요청이 동시에 들어와도 데이터 일관성 유지
3. **데이터 무결성**: 중간 상태 없이 완전한 상태 전환
4. **에러 복구**: 트랜잭션 실패 시 자동 롤백으로 이전 상태 유지