# 알람 스케줄러 함수 상세 설명

## 1. `loadAlarms(baseUrl: string)` 함수

이 함수는 서버 시작 시 모든 예정된 알람을 데이터베이스에서 로드하고 스케줄링합니다.

### 상세 동작
1. 데이터베이스에서 미래의 모든 미실행 알람을 조회합니다.
2. 각 알람에 대해 `scheduleAlarm` 함수를 호출하여 스케줄링합니다.
3. 스케줄링된 알람 수를 반환합니다.

### 실제 사용 예시
```typescript
// 서버 시작 시 실행
const baseUrl = 'http://localhost:3000';
const alarmsCount = await loadAlarms(baseUrl);
console.log(`서버가 ${alarmsCount}개의 알람을 스케줄링했습니다.`);
```

### 구체적인 예
서버가 재시작되면 이 함수가 실행됩니다. 예를 들어, 다음과 같은 약속 알람들이 데이터베이스에 저장되어 있다고 가정해 봅시다:

1. 사용자 A: 오늘 오후 3시 약속, 30분 전 알림 설정 (오후 2:30)
2. 사용자 B: 내일 오전 10시 약속, 1시간 전 알림 설정 (내일 오전 9:00)
3. 사용자 C: 어제 오후 6시 약속 (이미 지남)

`loadAlarms` 함수는 1번과 2번만 로드하고 스케줄링합니다. 3번은 이미 지난 시간이기 때문에 로드되지 않습니다.

## 2. `scheduleAlarm(alarm: any, baseUrl: string)` 함수

이 함수는 개별 알람을 Node.js 스케줄러에 등록하여 지정된 시간에 실행되도록 합니다.

### 상세 동작
1. 같은 알람 ID로 이미 예약된 작업이 있는지 확인하고, 있다면 취소합니다 (중복 방지).
2. `node-schedule` 라이브러리를 사용하여 알람의 `triggerAt` 시간에 실행될 작업을 예약합니다.
3. 알람이 트리거될 때:
   - 알람 상태를 DB에서 `isTriggered: true`로 업데이트
   - API 호출을 통해 푸시 알림 발송
   - 작업 완료 후 메모리에서 제거

### 실제 사용 예시
```typescript
// 특정 알람 스케줄링
const alarm = {
  id: 123,
  triggerAt: new Date('2023-12-01T14:30:00'),
  // 기타 필요한 속성들...
};
scheduleAlarm(alarm, 'http://localhost:3000');
```

### 구체적인 예
사용자가 "내일 오후 3시에 커피숍에서 만나자"라는 약속을 만들고 "30분 전 알림"을 설정했다고 가정해 봅시다:

1. 알람 객체가 생성되고 `triggerAt`은 "내일 오후 2:30"으로 설정됩니다.
2. `scheduleAlarm` 함수는 이 시간에 실행될 작업을 예약합니다.
3. 내일 오후 2:30이 되면:
   - 알람이 트리거됩니다.
   - DB에서 해당 알람의 상태가 `isTriggered: true`로 업데이트됩니다.
   - `/api/alarm/trigger` API가 호출되어 사용자에게 푸시 알림이 전송됩니다.
   - 사용자는 "약속 30분 전입니다: 커피숍"이라는 알림을 받게 됩니다.

## 3. `scheduleNewAlarm(alarmId: number, baseUrl: string)` 함수

이 함수는 새로 생성된 알람을 DB에서 로드하고 스케줄러에 등록하는 역할을 합니다.

### 상세 동작
1. 주어진 ID로 알람 정보를 데이터베이스에서 조회합니다.
2. 알람이 유효한지 확인합니다:
   - 존재하는지
   - 아직 트리거되지 않았는지 (`isTriggered: false`)
   - 트리거 시간이 미래인지
3. 유효하면 `scheduleAlarm` 함수를 호출하여 스케줄링합니다.
4. 스케줄링 성공 여부를 반환합니다.

### 실제 사용 예시
```typescript
// 새 알람 생성 후 스케줄링
const newAlarmId = await client.meetupAlarm.create({
  data: {
    userId: 42,
    chatMeetupId: 123,
    triggerAt: new Date('2023-12-15T09:00:00'),
    alarmTime: '1시간 전',
    isTriggered: false
  }
});

const scheduled = await scheduleNewAlarm(newAlarmId, 'http://localhost:3000');
console.log(scheduled ? '알람이 성공적으로 스케줄링되었습니다.' : '알람 스케줄링에 실패했습니다.');
```

### 구체적인 예
사용자가 앱에서 약속 알람을 처음 설정하는 상황을 가정해 봅시다:

1. 사용자가 약속 상세 페이지에서 "1시간 전 알림"을 선택합니다.
2. API 요청이 서버로 전송되고, DB에 새 알람 레코드가 생성됩니다(ID: 456).
3. API 핸들러에서 `scheduleNewAlarm(456, baseUrl)`을 호출합니다.
4. 이 함수는 DB에서 ID가 456인 알람을 조회합니다.
5. 알람의 `triggerAt`이 미래 시간인지 확인합니다.
6. 모든 조건이 충족되면 `scheduleAlarm` 함수를 호출하여 해당 알람을 스케줄링합니다.
7. 결과적으로 약속 시간 1시간 전에 사용자에게 알림이 전송됩니다.

## `baseUrl` 파라미터의 필요성

`baseUrl` 파라미터는 알람이 트리거될 때 서버가 자기 자신의 API 엔드포인트를 호출하기 위해 필요합니다.

### 주요 사용처
```typescript
// scheduleAlarm 함수 내부에서:
const response = await axios.post(`${baseUrl}/api/alarm/trigger`, {
  alarmId: alarm.id
});
```

### 필요한 이유

1. **서버-서버 통신**: 
   알람이 트리거될 때 스케줄러는 서버 자신의 API 엔드포인트를 호출하여 푸시 알림 전송 로직을 실행해야 합니다.

2. **환경 독립성**: 
   - 개발 환경: http://localhost:3000
   - 스테이징 환경: https://staging.example.com
   - 프로덕션 환경: https://example.com
   
   각 환경에 맞는 URL을 사용할 수 있도록 파라미터로 전달합니다.

3. **유연한 구성**: 
   환경 변수를 통해 URL을 동적으로 구성할 수 있어 다양한 배포 환경에서 코드 변경 없이 사용 가능합니다.

4. **아키텍처 분리**: 
   나중에 알람 서비스와 API 서버가 분리될 경우에도 쉽게 URL을 변경할 수 있습니다.