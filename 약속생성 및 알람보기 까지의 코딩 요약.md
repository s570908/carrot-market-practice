# 푸시 알림 시스템 개선 및 만료된 구독 처리

## 문제 상황

- 대량의 만료된 푸시 구독(147개+)이 데이터베이스에 누적됨
- 알림 발송 시 모든 구독에 대해 시도하여 많은 410 에러 발생
- 로그에 에러 메시지 폭주 (`push subscription has unsubscribed or expired`)
- 시스템 자원 낭비 및 불필요한 API 호출 증가

## 원인 분석

1. **브라우저/기기 변경**: 사용자가 다른 브라우저나 기기로 접속할 때마다 새 구독 생성
2. **구독 자동 만료**: 브라우저/운영체제 업데이트, 캐시 삭제 등으로 구독 만료
3. **만료된 구독 누적**: 만료 표시는 되지만 DB에서 삭제되지 않음

## 구현된 해결책

### 1. DB 정리 스크립트 (`/api/push/cleanup-expired`)
- 관리자용 엔드포인트로 만료된 구독을 일괄 정리
- 실행 방법: `GET /api/push/cleanup-expired?key=관리자키`

### 2. 알림 발송 로직 개선 (`callAlarmTrigger.ts`)
- ACTIVE 상태인 구독만 필터링하여 발송 시도 (`status === 'ACTIVE'`)
- 로깅 개선: 전체 구독 수, 활성 구독 수 기록
- 에러 처리 세분화: 410, 404 등 상태별 적절한 처리

### 3. 푸시 결과 요약 개선
- 에러 메시지 중복 제거 및 카운팅
- 개발 모드에서만 상세 결과 포함
- 구독 통계 정보 추가

### 4. 클라이언트 측 구독 모니터링 (`AppInitializer`)
- 페이지 포커스 시 구독 상태 확인
- 5분마다 자동 점검
- 만료된 구독 감지 시 자동으로 새 구독 생성

## 향후 개선 방향

1. **주기적 DB 정리**: 크론 작업으로 만료된 구독 자동 정리
2. **중복 구독 방지**: 동일 사용자의 유효한 구독이 이미 있으면 재활용
3. **구독 TTL 설정**: 일정 기간(예: 6개월) 후 강제 갱신
4. **지표 수집**: 구독 생성/만료/성공률 등 모니터링

## 사용 방법

1. 만료된 구독 정리: `/api/push/cleanup-expired?key=관리자키` 호출
2. 알림 발송 결과 모니터링: 로그에서 `총 구독 수`, `활성 구독 수` 확인
3. 정상 동작 확인: 대부분의 410 에러 로그가 사라지고, 유효한 구독으로만 알림 전송