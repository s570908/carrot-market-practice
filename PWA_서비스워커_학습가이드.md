# PWA + 서비스 워커 학습 가이드

이 문서는 PWA를 공부할 때 서비스 워커의 역할을 중심으로, 실제 프로젝트에서 바로 적용 가능한 순서로 정리한 학습 가이드입니다.

## 핵심 관점

가장 먼저 해야 할 것은 서비스 워커의 역할을 정확히 분리해서 이해하는 것입니다.

- PWA는 기능 묶음입니다.
- 서비스 워커는 그 핵심 실행 엔진입니다.

---

## 추천 학습 순서

1. 브라우저 동작 모델 이해
2. 서비스 워커 생명주기
3. 캐싱 전략
4. 오프라인 동작 설계
5. 푸시 알림
6. 설치 가능 PWA 조건
7. 디버깅/운영

---

## 1단계: 브라우저 동작 모델

먼저 아래 3가지를 명확히 이해합니다.

1. 페이지 스레드와 서비스 워커 스레드는 분리되어 있다.
2. 서비스 워커는 네트워크 요청을 가로채고 응답을 결정할 수 있다.
3. 서비스 워커는 백그라운드에서 이벤트 기반으로 동작한다.

### 학습 목표

- 서비스 워커를 단순 스크립트가 아닌 요청 제어 계층으로 이해한다.

---

## 2단계: 서비스 워커 생명주기

아래 흐름을 순서대로 익힙니다.

1. 등록
2. 설치
3. 활성화
4. fetch 이벤트 처리
5. 업데이트와 교체

여기서 막히면 이후 캐시/오프라인 설계가 꼬입니다.

### 학습 목표

- 각 단계에서 어떤 코드가 실행되고, 언제 사용자에게 반영되는지 설명할 수 있다.

---

## 3단계: 캐싱 전략부터 실습

전략별 적용 포인트를 구분합니다.

1. Cache First: 정적 리소스에 유리
2. Network First: API 응답 최신성에 유리
3. Stale While Revalidate: UX와 최신성 균형

현재 프로젝트의 테스트 페이지로 상태 관찰을 시작합니다.

- pages/pwa-tester.tsx

### 학습 목표

- 화면/리소스/API 각각에 어떤 전략이 맞는지 판단할 수 있다.

---

## 4단계: 오프라인 시나리오 설계

기능 우선순위 관점으로 설계합니다.

1. 오프라인에서도 반드시 보여야 할 화면 정의
2. 실패 시 대체 응답 규칙 정의
3. 개발자도구 Offline 모드로 실제 검증

### 학습 목표

- 오프라인 실패를 기능 결함이 아닌 설계 대상로 다룰 수 있다.

---

## 5단계: 푸시 알림

아래 흐름을 엔드투엔드로 익힙니다.

1. Notification 권한 흐름
2. Push Subscription 생성/갱신/만료 처리
3. 서버 발송 실패 시 재구독 UX

현재 프로젝트는 점검 UI가 있어 학습하기 좋습니다.

- pages/pwa-tester.tsx

### 학습 목표

- 구독 만료, 권한 거부, 엔드포인트 변경 같은 운영 이슈를 스스로 복구할 수 있다.

---

## 6단계: 설치 가능한 PWA 조건

설치 프롬프트가 뜨는 전제조건을 점검합니다.

1. 올바른 manifest
2. HTTPS
3. 서비스 워커 등록
4. beforeinstallprompt 흐름 이해

### 학습 목표

- 왜 설치 프롬프트가 뜨지 않는지 조건 기반으로 빠르게 진단할 수 있다.

---

## 7단계: 운영 관점

운영 안정성을 위해 아래를 갖춥니다.

1. 로그 체계: 등록, 설치, 활성, fetch hit/miss, push 결과
2. 캐시 버전 정책
3. 구독 만료 자동복구 정책

### 학습 목표

- 개발 단계에서 동작하던 기능을 운영 환경에서도 지속적으로 안정화할 수 있다.

---

## 실전 시작 루틴

아래 순서대로 진행하면 학습 효율이 높습니다.

1. 서비스 워커 생명주기 로그를 먼저 눈으로 확인
2. fetch 이벤트에 캐시 전략 1개만 적용
3. Offline 모드에서 실제 동작 확인
4. 푸시 알림 유효성 검사와 재구독 처리 추가
5. 설치 프롬프트 UX 다듬기

---

## 체크리스트

### 기초

- [ ] 서비스 워커 생명주기 5단계를 말로 설명할 수 있다.
- [ ] 각 단계에서 문제 발생 시 확인할 로그 위치를 안다.

### 캐시/오프라인

- [ ] 정적 리소스, API에 서로 다른 캐시 전략을 적용했다.
- [ ] 오프라인에서 필수 화면이 정상 동작한다.

### 푸시

- [ ] 권한 상태 변화(허용/거부)에 대응한다.
- [ ] 구독 만료 또는 엔드포인트 변경 시 복구 UX가 있다.

### 설치/운영

- [ ] 설치 조건(Manifest, HTTPS, SW 등록)을 점검했다.
- [ ] 운영 로그와 캐시 버전 정책을 정의했다.

---

## 다음 학습 제안

현재 프로젝트 기준으로 하루 1시간, 1주 학습 플랜을 만들면 체감 성장이 빠릅니다.

원하면 다음 문서로 아래를 이어서 작성합니다.

1. 1일차부터 7일차까지의 실습 과제
2. 각 과제의 완료 기준
3. 실패했을 때 디버깅 체크포인트
