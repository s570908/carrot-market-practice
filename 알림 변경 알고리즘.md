## 질문
```js
enum AlarmStatus {
  SCHEDULED
  SENT
  FAILED
  CANCELED
}

model AlarmSetting {
  id           Int         @id @default(autoincrement())
  userId       Int
  messageId    Int // 알림 메시지 ID (SYSTEM 메시지)
  alarmTime    String // "10분 전", "30분 전" 등
  triggerAt    DateTime // 실제 알림이 울려야 할 시간
  // ChatMeetup와의 관계를 명확히 설정 (N:1, 여러 AlarmSetting이 하나의 ChatMeetup을 참조)
  chatRoomId   Int
  chatMeetupId Int // 약속 ID 추가
  status       AlarmStatus @default(SCHEDULED)
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @default(now()) @updatedAt

  user       User       @relation("UserToAlarmSetting", fields: [userId], references: [id], onDelete: Cascade)
  sellerChat SellerChat @relation("SellerChatToAlarmSetting", fields: [messageId], references: [id], onDelete: Cascade)
  chatMeetup ChatMeetup @relation(fields: [chatMeetupId], references: [id], onDelete: Cascade)

  @@unique([messageId, userId]) // 이 제약 조건 유지
  @@index([chatRoomId])
  @@index([triggerAt, status]) // 알림 트리거 시간과 상태로 인덱스 최적화
  @@index([chatMeetupId]) // ChatMeetup 관련 쿼리 최적화
}
```

에서 alarmsetting은 각각의 인스턴스에 대해서 status가 4가지가 있다. 각각의 경우에 대해서 alarmsetting이 변경되거나 추가되는 경우에 대해서 최적의 알고리즘을 제시해줘

AlarmSetting의 상태(`status`: SCHEDULED, SENT, FAILED, CANCELED)와 약속(알림) 변경/추가 시 **최적의 알고리즘**은 다음과 같습니다.

---

## 1. **알림 추가(새 약속 생성)**
- **조건:** 해당 chatRoomId에 SCHEDULED 상태의 AlarmSetting이 있는지 확인
- **알고리즘:**
    1. **기존 SCHEDULED 알림 조회**
        - `findFirst({ where: { chatRoomId, status: SCHEDULED }, orderBy: { triggerAt: 'desc' } })`
    2. **기존 알림이 있으면**
        - **(a)** 기존 알림의 messageId와 새로 생성될 messageId가 다르면
            - 기존 알림을 **CANCELED**로 업데이트
    3. **새 AlarmSetting 생성**
        - status: SCHEDULED, triggerAt: 계산된 시간

---

## 2. **알림 변경(알림 시간만 변경)**
- **조건:** 기존 AlarmSetting이 SCHEDULED 상태인지 확인
- **알고리즘:**
    1. **기존 SCHEDULED 알림 조회**
    2. **기존 알림이 있으면**
        - **(a)** 기존 알림의 messageId와 변경 대상 messageId가 다르면
            - 기존 알림을 **CANCELED**로 업데이트
        - **(b)** 기존 알림의 messageId와 같으면
            - 기존 알림의 alarmTime, triggerAt만 **업데이트**
    3. **변경된 알림이 없다면 새로 생성**

---

## 3. **알림 취소**
- **조건:** 사용자가 알림을 "알림 없이"로 변경하거나 약속을 삭제
- **알고리즘:**
    1. **기존 SCHEDULED 알림 조회**
    2. **존재하면 status를 CANCELED로 업데이트**

---

## 4. **알림 발송(SENT), 실패(FAILED)**
- **조건:** 백엔드에서 실제 알림 발송 시
- **알고리즘:**
    1. **알림 발송 성공 시**
        - 해당 AlarmSetting의 status를 SENT로 업데이트
    2. **알림 발송 실패 시**
        - 해당 AlarmSetting의 status를 FAILED로 업데이트

---

## 5. **알림 재설정/재예약**
- **조건:** FAILED 상태의 알림을 재시도할 때
- **알고리즘:**
    1. **FAILED 상태의 AlarmSetting 조회**
    2. **재시도 시 status를 SCHEDULED로 변경, triggerAt 재설정**

---

## **정리: 상태별 처리 플로우**

| 상황                | 기존 SCHEDULED 있음 | messageId 같음 | 처리 방법                                 |
|---------------------|--------------------|---------------|-------------------------------------------|
| 새 알림 추가        | O                  | X             | 기존 알림 CANCELED, 새 알림 SCHEDULED 생성 |
| 새 알림 추가        | O                  | O             | (중복 생성 방지) 무시 또는 업데이트        |
| 새 알림 추가        | X                  | -             | 새 알림 SCHEDULED 생성                    |
| 알림 시간만 변경    | O                  | O             | alarmTime, triggerAt만 업데이트           |
| 알림 시간만 변경    | O                  | X             | 기존 알림 CANCELED, 새 알림 SCHEDULED 생성 |
| 알림 취소           | O                  | -             | 기존 알림 CANCELED                        |
| 알림 발송/실패      | O                  | -             | status SENT/FAILED로 업데이트             |

---

## **예시 코드 (Prisma 기준)**

````typescript
// ...existing code...
// AlarmSetting 추가/변경 알고리즘 예시

async function upsertAlarmSetting({
  chatRoomId,
  chatMeetupId,
  messageId,
  userId,
  alarmTime,
  triggerAt,
}: {
  chatRoomId: number;
  chatMeetupId: number;
  messageId: number;
  userId: number;
  alarmTime: string;
  triggerAt: Date;
}) {
  // 1. 기존 SCHEDULED 알림 조회
  const existing = await prisma.alarmSetting.findFirst({
    where: { chatRoomId, status: "SCHEDULED" },
    orderBy: { triggerAt: "desc" },
  });

  if (existing) {
    if (existing.messageId !== messageId) {
      // 기존 알림 취소
      await prisma.alarmSetting.update({
        where: { id: existing.id },
        data: { status: "CANCELED" },
      });
      // 새 알림 생성
      return prisma.alarmSetting.create({
        data: {
          chatRoomId,
          chatMeetupId,
          messageId,
          userId,
          alarmTime,
          triggerAt,
          status: "SCHEDULED",
        },
      });
    } else {
      // messageId 같으면 alarmTime/triggerAt만 업데이트
      return prisma.alarmSetting.update({
        where: { id: existing.id },
        data: { alarmTime, triggerAt },
      });
    }
  } else {
    // 기존 알림 없음: 새로 생성
    return prisma.alarmSetting.create({
      data: {
        chatRoomId,
        chatMeetupId,
        messageId,
        userId,
        alarmTime,
        triggerAt,
        status: "SCHEDULED",
      },
    });
  }
}
// ...existing code...
````

---

## **핵심 요약**
- **SCHEDULED**는 항상 1개만 유지(동일 chatRoomId 기준)
- 새 알림 추가/변경 시 기존 SCHEDULED는 **CANCELED** 처리
- messageId가 같으면 **업데이트**, 다르면 **새로 생성**
- 취소/삭제 시 **CANCELED** 처리
- 발송/실패 시 **SENT/FAILED** 처리

이 방식이 데이터 일관성과 성능 모두에 최적입니다.