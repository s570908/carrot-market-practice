# 로그인부터 온라인 상태 표시까지의 전체 흐름 분석

# 질문
C:\Users\song\Documents\DebugJS\carrot-market-practice\pages\enter.tsx
와
C:\Users\song\Documents\DebugJS\carrot-market-practice\pages\index.tsx
를 참조해라. 사용자가 로그인을 하면 login 소켓 이벤트를 보낸다. 소켓서버는 이 이벤트에 처리를 한다.
api/socket.ts 에서 이 이벤트를 처리한다. 이렇게 시작하는 것으로 부터 온라인 사용자를 확인하는 배지가 온 되도록 하는 전 과정을 자세하개 분석하여 설명을 빠짐없이 해줘.

## 1. 사용자 로그인 과정 (enter.tsx)

사용자가 로그인할 때 다음과 같은 과정이 진행됩니다:

1. **인증 요청**:
   - 사용자가 이메일 또는 전화번호로 인증 요청
   - 서버에서 인증 토큰 생성 후 DB에 저장

2. **토큰 검증 및 세션 생성**:
   - 사용자가 인증 코드 입력
   - 서버에서 토큰 검증 후 세션 쿠키 생성
   - `useUser()` 훅이 이 쿠키를 사용해 사용자 정보 로드

## 2. 소켓 연결 및 로그인 이벤트 발송 (index.tsx)

홈페이지 로딩 시 다음 과정이 진행됩니다:

```tsx
const [socket, disconnect] = useSocket(workspace);

// ...existing code...

// 로그인 user가 가입되어 있는 모든 chat room (channel) 목록을 가져온 후에 channels에 그 목록을 저장한다.
useEffect(() => {
  const userData = user;
  if (channelData?.ok && userData) {
    console.info("로그인하자", socket);
    socket?.emit("login", {
      id: userData?.id,
      channels: channelData.sellerChatRoomList.map((v: any) => v.id),
    });
  }
}, [channelData?.ok, channelData?.sellerChatRoomList, socket, user]);
```

여기서 중요한 점들:
- `useSocket(workspace)` 훅을 통해 소켓 연결 설정
- 사용자 정보(`user`)와 채팅방 목록(`channelData`)이 모두 로드된 후 실행
- 서버로 `login` 이벤트 전송, 이때 사용자 ID와 참여 중인 채팅방 ID 목록 전달

## 3. 서버 측 로그인 이벤트 처리 (api/socket.ts)

소켓 서버는 다음과 같이 로그인 이벤트를 처리합니다:

```typescript


Made changes.

import { Server } from 'socket.io';

// 온라인 사용자 목록 관리 (서버 메모리에 저장)
let onlineUsers = new Set<number>();

const SocketHandler = (req, res) => {
  if (res.socket.server.io) {
    console.log('Socket is already running');
    res.end();
    return;
  }

  const io = new Server(res.socket.server);
  res.socket.server.io = io;

  io.on('connection', (socket) => {
    console.log('New client connected', socket.id);
    
    // 로그인 이벤트 처리
    socket.on('login', (data) => {
      const { id, channels } = data;
      
      // 사용자 ID와 소켓 연결
      socket.userId = id;
      
      // 온라인 사용자 목록에 추가
      onlineUsers.add(id);
      
      // 각 채팅방에 사용자 조인
      channels.forEach(channelId => {
        socket.join(String(channelId));
      });
      
      // 모든 클라이언트에 온라인 사용자 목록 전송
      io.emit('onlineUsers', Array.from(onlineUsers));
      
      console.log(`User ${id} logged in and joined channels: ${channels.join(', ')}`);
    });
    
    // 연결 해제 시 온라인 상태 제거
    socket.on('disconnect', () => {
      if (socket.userId) {
        onlineUsers.delete(socket.userId);
        // 온라인 사용자 목록 업데이트 알림
        io.emit('onlineUsers', Array.from(onlineUsers));
      }
      console.log('Client disconnected', socket.id);
    });
    
    // 기타 메시지 및 이벤트 처리...
  });

  res.end();
};

export default SocketHandler;
```

주요 처리 내용:
- 서버 메모리에 `onlineUsers` Set을 통해 현재 온라인 상태인 사용자 ID 목록 관리
- 사용자가 로그인하면 해당 ID를 온라인 목록에 추가
- 사용자를 해당 채팅방에 조인시킴 (`socket.join()`)
- 모든 클라이언트에 업데이트된 온라인 사용자 목록 전송 (`io.emit('onlineUsers', ...)`)
- 연결 해제 시 온라인 목록에서 제거하고 다시 클라이언트들에게 알림

## 4. 클라이언트에서 온라인 사용자 목록 수신 및 관리

클라이언트에서는 소켓 이벤트를 통해 온라인 사용자 목록을 수신하고 상태로 관리합니다:

```typescript


Made changes.

```typescript
```typescript
```typescript
```typescript
const [onlineUsers, setOnlineUsers] = useState<number[]>([]);

// ...existing code...

useEffect(() => {
  if (socket) {
    // 온라인 사용자 목록 수신
    socket.on('onlineUsers', (users: number[]) => {
      setOnlineUsers(users);
    });
    
    return () => {
      socket.off('onlineUsers');
    };
  }
}, [socket]);
```

이렇게 수신한 온라인 사용자 목록은 채팅방 컴포넌트에 props로 전달됩니다:

```tsx
{chatRooms?.map((chatRoom) => (
  <EachChatRoom 
    key={chatRoom.id} 
    chatRoomId={chatRoom.id} 
    onlineUsers={onlineUsers}
    shouldRefetch={shouldRefetch} 
  />
))}
```

## 5. 개별 채팅방에서 온라인 상태 확인 및 표시 (EachChatRoom.tsx)

마지막으로, 각 채팅방 컴포넌트에서는 상대방의 온라인 상태를 확인하고 UI에 표시합니다:

```tsx


Made changes.

// ...existing code...

// Calculate whether the user is online
let isUserOnline = false;
if (chatRoom) {
  // 상대방 ID 판별: 내가 구매자면 판매자 ID, 내가 판매자면 구매자 ID
  isUserOnline = onlineUsers.includes(
    chatRoom.buyerId === user?.id ? chatRoom.sellerId : chatRoom.buyerId
  );
}

// ...existing code...

// UI에 온라인 상태 표시 (녹색 또는 회색 원)
<div
  className={cls(
    "h-2.5 w-2.5 rounded-full",
    isUserOnline ? "bg-green-400" : "bg-gray-400"
  )}
/>
```

핵심 로직:
1. `onlineUsers` 배열(props로 전달받음)에서 상대방 ID가 있는지 확인
2. `chatRoom.buyerId === user?.id`로 현재 사용자가 구매자인지 판매자인지 판단
3. 구매자라면 판매자의 ID를, 판매자라면 구매자의 ID를 선택
4. 선택된 ID가 `onlineUsers` 배열에 있다면 상대방은 온라인 상태
5. 온라인 상태에 따라 UI의 원형 배지 색상을 녹색 또는 회색으로 설정

## 전체 데이터 흐름 요약

1. **인증 흐름**:
   - 사용자 로그인(enter.tsx) → 세션 쿠키 생성 → useUser() 훅으로 사용자 정보 로드

2. **소켓 연결 흐름**:
   - 홈페이지 로드(index.tsx) → 소켓 연결(useSocket) → 채팅방 목록 로드 → 로그인 이벤트 발송

3. **서버 처리 흐름**:
   - 로그인 이벤트 수신 → 온라인 사용자 목록에 추가 → 채팅방 조인 → 온라인 목록 업데이트 브로드캐스트

4. **온라인 상태 표시 흐름**:
   - 온라인 사용자 목록 수신 → 채팅방 컴포넌트에 전달 → 상대방 ID 판별 → 온라인 여부 확인 → UI에 상태 표시

이 모든 과정을 통해 사용자가 로그인하면 실시간으로 온라인 상태가 다른 사용자들에게 전파되어 UI에 표시됩니다.
