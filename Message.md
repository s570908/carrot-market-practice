# Message 컴포넌트 개요

## 목적
`Message` 컴포넌트는 당근마켓 애플리케이션에서 채팅 메시지를 표시하며, 다양한 메시지 타입, 약속 관리, 인터랙티브 액션을 지원합니다.

## Props
- `message`: 메시지의 텍스트 내용
- `reversed`: 메시지 레이아웃을 반전(송신자/수신자 구분)
- `name`: 표시할 사용자 이름
- `avatar`: 사용자 아바타 이미지 URL
- `date`: 메시지의 타임스탬프
- `isAppointment`: 약속 데이터 포함 여부
- `appointmentData`: 약속(미팅) 정보(시간, 장소, 좌표 등)
- `messageType`: 메시지 타입 (USER 또는 SYSTEM)
- `actions`: 메시지에 부착된 버튼/링크 등 인터랙티브 요소
- `chatRoomId`: 채팅방 ID

## 주요 기능
- **유저 메시지**: 아바타, 이름, 시간과 함께 일반 채팅 말풍선 표시
- **시스템 메시지**: 알림/시스템 이벤트용 중앙 정렬 회색 메시지
- **약속 관리**: 약속 정보 표시 및 편집 지원
- **인터랙티브 액션**: 버튼/링크 및 툴팁 지원
- **시간 포맷팅**: 메시지 및 약속에 한국 시간대 적용

## 주요 구현 세부사항
- `useAwaitableModal` 훅을 통한 약속 편집 모달 사용
- 액션 버튼의 비활성화 및 툴팁 처리
- 송신/수신자에 따라 반응형 레이아웃 지원
- dayjs를 활용한 한국어 날짜/시간 로컬라이징

## 사용 예시
이 컴포넌트는 채팅 인터페이스에서 유저 간 대화, 시스템 알림, 인터랙티브 요소 등 다양한 메시지 표시 시나리오를 처리합니다.

# Message 컴포넌트 블럭별 요약

## import 및 설정 (1~17행)
```md
- 유틸 함수, React 컴포넌트, 외부 라이브러리 import
- dayjs를 활용한 타임존 및 한국어 로케일 설정
- 약속(appointment) 관리를 위한 모달 설정
```

## 타입 정의 (19~42행)
```md
- `MessageAction`: 버튼/링크 등 인터랙티브 요소의 라벨, 핸들러 정의
- `MessageProps`: 컴포넌트 props 정의
  - 메시지 텍스트, 발신자 정보, 타임스탬프
  - 레이아웃 제어(reversed)
  - 약속 데이터 구조
  - 시스템/유저 메시지 타입
  - 인터랙티브 액션 설정
```

## 컴포넌트 세팅 (44~63행)
```md
- props 구조분해 할당 및 초기화
- 약속 편집 모달 훅 세팅
- 채팅방 컨텍스트 준비
```

## 헬퍼 함수 (65~75행)
```md
- `formatKoreanTime`: UTC 타임스탬프를 한국 시간대로 변환
- 날짜/시간 포맷 문자열 반환
```

## 약속 핸들러 (77~89행)
```md
- `handleShowAppointment`: 약속 모달 오픈 및 검증
- 약속 데이터를 모달에 전달
- 채팅방 ID 누락 시 경고 처리
```

## 시스템 메시지 렌더러 (91~174행)
```md
- 시스템 알림 메시지를 특수 스타일로 렌더링
- 중앙 정렬된 회색 말풍선 생성
- 액션 버튼/링크 및 툴팁 처리
- 비활성화 상태에 대한 설명 툴팁 지원
```

## 유저 메시지 렌더러 (176~306행)
```md
- 일반 채팅 메시지(아바타, 유저명) 렌더링
- 발신자에 따라 말풍선 정렬/방향 조정
- 약속 정보가 있을 경우 상세 표시
- 메시지별 액션 버튼 렌더링
- 타임스탬프 포맷팅 및 표시
- 비활성화 액션에 대한 툴팁 처리
```

## 인터랙티브 요소 (203~264행 & 267~303행)
```md
- 메시지에 부착된 버튼/링크 렌더링
- 비활성화 상태 시 시각적 피드백 제공
- 툴팁으로 추가 설명 제공
- 클릭 이벤트 및 전파 제어
```