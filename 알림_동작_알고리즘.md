## 클라이언트가 새로운 알람을 세팅하고 알림이 울리게 되는 전체 과정

### 1. 클라이언트에서 알람 설정 요청

- 사용자가 채팅방에서 "알림설정" 버튼을 클릭
- ActionSheet 또는 AppointmentEditModal에서 알림 시간(alarmTime) 선택
- 클라이언트에서 `writeAlarmSettings` API 함수 호출
  - `/api/alarm-settings/[chatRoomId]`에 PUT 요청
  - 파라미터: chatId, alarmTime, triggerAt(알림 울릴 실제 시각), disableAlarm 등

### 2. 서버에서 알람 설정 처리

- Next.js API 라우트 `/pages/api/alarm-settings/[chatRoomId]/index.ts`에서 요청 수신
- PUT 요청 처리:
  - 해당 chatRoomId+userId 조합의 AlarmSetting이 있으면 업데이트, 없으면 새로 생성 (upsert)
  - DB에 alarmTime, triggerAt, status(SCHEDULED) 등 저장
- 알람 생성/수정 후 `scheduleAlarmById(alarmId, baseUrl)` 호출
  - node-schedule로 triggerAt 시각에 알림 예

### 3. 서버에서 알람 예약

- `libs/server/alarmScheduler.ts`의 `scheduleAlarm` 함수에서
  - triggerAt 시각에 node-schedule Job 등록
  - Job은 activeJobs Map에 저장되어 관리됨

### 4. 알람 트리거 시점

- 예약된 triggerAt 시각이 되면 node-schedule Job이 실행됨
- `callAlarmTrigger({ baseUrl, alarmId })` 함수 호출
  - 실제 알림 발송(푸시, SMS, 이메일 등)
  - DB의 AlarmSetting 상태를 SENT로 변경

### 5. 클라이언트 알림 수신

- 푸시 알림 등으로 사용자에게 알림이 울림
- 알림이 울린 후, 클라이언트에서 알림 상태를 확인하거나 UI를 갱신할 수 있음

---

## 전체 흐름 요약

1. **클라이언트**: 알림 시간 선택 → API 호출
2. **서버**: 알람 DB 저장 → node-schedule로 예약
3. **서버**: 예약된 시간에 알림 발송(Job 실행)
4. **사용자**: 푸시 등으로 알림 수신

---

## 보조 설명

- 서버가 재시작되면 `initializeAlarmScheduler`로 모든 미래 알람을 다시 예약
- 알람 취소/수정 시에는 기존 Job을 취소하고 새로 예약
- 알림 발송 실패/성공 여부는 로그 및 DB 상태로 관리

---

## 알림 시간 설정 UI(모달)와 각 계층의 역할

### 1. **모달(알림 설정 UI)의 역할**
- 사용자가 알림 시간을 선택할 수 있도록 인터페이스 제공
- 예시: "10분 전", "30분 전", "1시간 전", "없음" 등 옵션 표시
- 사용자가 값을 선택하면 부모 컴포넌트에 결과를 전달
- UX 개선: 잘못된 시간(과거 등) 선택 시 즉시 안내/비활성화

### 2. **모달의 부모 컴포넌트의 역할**
- 모달을 열고 닫는 상태 관리
- 모달에서 선택된 알림 시간 값을 받아서 서버에 API 요청
  - 예: writeAlarmSettings, updateAlarmSettings 등 호출
- 알림 설정 성공/실패 시 UI 갱신(알림 상태, 메시지 등)
- 서버 응답에 따라 데이터 리패칭, 에러 안내 등 처리

### 3. **서버의 역할**
- 클라이언트로부터 알림 시간 설정 요청을 받음 (PUT/POST 등)
- 알림 시간이 유효한지(과거/미래) 최종 검증
- DB에 알림 정보 저장/업데이트 (AlarmSetting)
- node-schedule 등으로 알림 트리거 예약
- 알림 시간이 되면 실제 푸시/소켓 등으로 알림 발송
- 에러/예외 상황 처리(잘못된 시간, 중복 알림 등)

---

## 전체 흐름 요약

1. **모달**: 사용자 입력/선택 → 부모에 결과 전달
2. **부모**: 모달 결과 받아 서버에 요청 → UI 갱신
3. **서버**: 데이터 검증/저장/알림 예약/발송

---

**정리:**  
- 모달: UI/입력 담당  
- 부모: 상태/API/데이터 갱신 담당  
- 서버: 검증/저장/알림 예약/발송 담당  
→ 각 계층이 역할을 분리하여 안정적이고 일관된 알림 기능을 제공합니다.

---

**이 과정으로 사용자가 원하는 시간에 정확하게 알림이 울리게 됩니다.**

### 코드 설명:  
```typescript
triggerAt: new Date(
  new Date(appointmentTime).getTime() - 30 * 60 * 1000
).toISOString()
```

#### 의미

- `appointmentTime`은 약속(미팅)의 실제 시작 시각(예: "2024-06-10T15:00:00Z")입니다.
- `new Date(appointmentTime)`  
  → 문자열 또는 Date 객체인 appointmentTime을 Date 객체로 변환합니다.
- `.getTime()`  
  → 해당 시각을 "1970년 1월 1일 00:00:00 UTC"부터의 밀리초(ms)로 변환합니다.
- `30 * 60 * 1000`  
  → 30분을 밀리초로 환산한 값입니다.  
    (30분 × 60초 × 1000ms = 1,800,000ms)
- `new Date(... - 1,800,000)`  
  → 약속 시각에서 1,800,000ms(=30분)를 뺀 새로운 Date 객체를 만듭니다.
- `.toISOString()`  
  → 그 Date 객체를 ISO 8601 문자열(예: "2024-06-10T14:30:00.000Z")로 변환합니다.

#### 결론

- **약속 시각(appointmentTime)에서 30분 전의 시각을 계산하여,  
  그 시각을 알림이 울릴 시각(triggerAt)으로 저장**하는 코드입니다.
- 즉, 약속이 15:00이면 triggerAt은 14:30이 됩니다.

#### 실무적 의미

- 이 값은 알림 스케줄러가 "언제 알림을 울릴지"를 정확히 지정하는 데 사용됩니다.
- 예: "약속 30분 전에 알림"을 구현할 때 필수적인 계산입니다.

---

### [문제 분석]  
**WebPushError: Received unexpected response code (410)**  
- 메시지: `push subscription has unsubscribed or expired.`
- 구독(Subscription) 상태가 만료(EXPIRED) 또는 사용자가 알림을 해제(unsubscribed)한 상태임을 의미
- 코드에서 "Subscription 115 marked as EXPIRED due to 410 Gone." 로그가 반복적으로 출력됨

---

### [원인]
1. **만료된(410 Gone) 구독이 DB에 남아 있음**
   - 푸시 발송 시마다 같은 만료된 구독에 계속 푸시를 시도
   - 410 Gone 응답을 받고, 상태를 EXPIRED로 업데이트
   - 하지만 다음 알림 트리거 때도 여전히 이 구독이 ACTIVE로 간주되어 반복 발송 → 무한 루프

2. **구독 상태 관리 로직의 문제**
   - 이미 EXPIRED로 마킹된 구독을 다음 알림 발송 시에도 ACTIVE로 잘못 필터링하거나,
   - DB에서 실제로 삭제하지 않고 상태만 바꿔도, ACTIVE만 발송 대상으로 필터링하지 않으면 계속 시도

---

### [해결 방법]
- **푸시 발송 대상 필터링 강화**
  - 반드시 `status === 'ACTIVE'`인 구독에만 푸시 발송
  - 이미 EXPIRED/INVALID 등으로 마킹된 구독은 발송 대상에서 제외

- **구독 삭제 또는 상태 업데이트 후 재시도 방지**
  - 410 Gone 등 만료 에러 발생 시, 구독을 DB에서 삭제하거나,  
    최소한 status를 'EXPIRED'로 바꾸고,  
    다음 발송 시 ACTIVE만 대상으로 삼도록 쿼리/필터링

- **코드 예시**
    ```typescript
    // 푸시 발송 전
    const activeSubscriptions = subscriptions.filter(sub => sub.status === 'ACTIVE');
    // ...activeSubscriptions만 대상으로 발송...
    ```

- **이미 EXPIRED로 마킹된 구독이 ACTIVE로 남아있는지 DB 확인 필요**
  - DB에 status 컬럼이 제대로 반영되는지,  
    혹은 상태 업데이트 후에도 캐시/쿼리 문제로 ACTIVE로 남아있는지 점검

---

### [요약]
- 410 Gone(만료) 구독에 계속 푸시를 시도해서 무한 루프가 발생
- **ACTIVE 상태만 대상으로 발송**하도록 코드/DB를 점검해야 함
- 만료된 구독은 즉시 삭제하거나, 최소한 status를 'EXPIRED'로 바꾼 뒤  
  다음 발송 시 대상에서 제외해야 함

### 410 Gone 에러 원인 분석

**WebPushError: Received unexpected response code (statusCode: 410)**

#### 원인
1. **FCM 구독이 만료됨**
   - 사용자가 브라우저에서 알림 권한을 해제했거나
   - 브라우저 데이터를 삭제했거나
   - FCM에서 해당 구독을 자동으로 만료시킴

2. **중복 상태 업데이트**
   - 현재 코드에서 같은 구독에 대해 두 번 EXPIRED로 마킹하고 있음:
     ```
     Subscription 157 marked as EXPIRED due to 410 Gone.
     Subscription 157 marked as EXPIRED due to push error.
     ```

#### 해결 방법

1. **try-catch 블록에서 중복 처리 제거**
2. **에러 처리 통합**

---

## PushSubscriptionManager 적용 위치 안내

### 1. **PushSubscriptionManager란?**
- 사용자의 웹 푸시 구독 상태(만료, 유효, 거부 등)를 감지하고
- 만료 시 자동으로 재구독을 유도하거나 안내하는 컴포넌트입니다.

---

### 2. **적용 위치(권장)**

#### (1) **앱의 루트 레이아웃 또는 공통 레이아웃**
- `/pages/_app.tsx` 또는 `/components/Layout.tsx` 등
- 모든 페이지에서 한 번만 렌더링되도록 최상위에 배치
- 예시:
  ```tsx
  // filepath: /Users/gimgyeong-o/carrot-market-practice/pages/_app.tsx
  import PushSubscriptionManager from "@/components/PushSubscriptionManager";
  // ...existing code...
  function MyApp({ Component, pageProps }) {
    return (
      <>
        <PushSubscriptionManager />
        <Component {...pageProps} />
      </>
    );
  }
  export default MyApp;
  ```

#### (2) **알림이 중요한 주요 페이지**
- 예: 채팅방(`/pages/chats/[id].tsx`), 약속 생성/수정 페이지 등
- 해당 페이지의 최상단(또는 Layout 내부)에 추가

#### (3) **사용자 프로필/설정 페이지**
- 사용자가 직접 알림 구독 상태를 확인/갱신할 수 있도록 별도 UI로 제공

---

### 3. **요약**

- **최적 위치:** `/pages/_app.tsx` 또는 `/components/Layout.tsx`  
  → 모든 페이지에서 자동으로 구독 상태를 감지/갱신
- **추가 안내:** 알림이 중요한 페이지(채팅방 등)에서 중복 렌더링해도 무방(내부적으로 중복 처리됨)

---

**이렇게 하면 FCM 구독 만료 시 사용자에게 자동 안내 및 재구독 유도가 가능합니다.**

---

### VAPID 키와 410 Gone(구독 만료) 에러의 관계

- `NEXT_PUBLIC_VAPID_PUBLIC_KEY`와 `VAPID_PRIVATE_KEY`는 **웹 푸시(Web Push)에서 서버와 브라우저 간의 신뢰를 보장**하는 공개키/비밀키 쌍입니다.
- 이 키는 **구독(Subscription) 생성/유지와 직접적인 만료(410 Gone) 원인과는 무관**합니다.

#### 410 Gone(만료) 에러의 주요 원인
- 사용자가 브라우저 알림 권한을 해제
- 브라우저 캐시/데이터 삭제
- FCM(구글)에서 구독을 만료 처리
- 브라우저/OS 업데이트 등

#### VAPID 키가 만료/변경되어야 하는 경우
- **키가 유출된 경우** (보안상 위험)
- **키를 잘못 설정한 경우** (브라우저에서 구독 생성 자체가 안 됨)
- **서버/도메인 변경 등으로 새 키가 필요한 경우**

#### 410 Gone과의 관계
- 이미 생성된 구독은 **VAPID 키를 바꿔도 기존 구독에는 영향이 없음**
- 즉, 410 Gone은 **구독 자체의 만료**이므로, 키를 새로 발급해도 기존 만료 구독은 살아나지 않음

---

### VAPID 키 재발급 방법

1. **node.js에서 web-push 패키지 사용**
   ```bash
   npx web-push generate-vapid-keys
   ```
   또는
   ```js
   // node REPL 또는 별도 스크립트에서
   const webpush = require('web-push');
   console.log(webpush.generateVAPIDKeys());
   ```

2. **결과 예시**
   ```
   Public Key:  BExxxxx...
   Private Key: xxxxx...
   ```

3. **.env 파일에 반영**
   ```
   NEXT_PUBLIC_VAPID_PUBLIC_KEY=...
   VAPID_PRIVATE_KEY=...
   ```

4. **주의**
   - 키를 새로 발급하면 **기존 구독은 모두 무효**가 되므로,  
     모든 사용자가 다시 구독을 해야 알림을 받을 수 있습니다.

---

### 결론

- 410 Gone은 VAPID 키와 직접적 관련이 없음(키를 바꿔도 기존 만료 구독은 복구 불가)
- 키가 유출/오류/도메인 변경 등 특별한 사유가 아니면 재발급할 필요 없음
- 키를 재발급하면 모든 사용자가 다시 구독해야 함(서비스 영향 주의)

---

## 질문 요약
- `<PushSubscriptionManager />`를 주석 처리(비활성화)하니 410 Gone 에러가 사라진 이유?
- 주석 처리 전, PushSubscriptionManager가 만료된 구독을 지우는 동작을 했는가?

---

### 답변

#### 1. PushSubscriptionManager의 역할
- PushSubscriptionManager는 클라이언트에서 **브라우저의 푸시 구독 상태를 감지**하고,
- 만료/비정상 구독이면 **구독을 해제(unsubscribe)하고 새로 생성**하거나,
- 서버에 구독 정보를 갱신/삭제 요청을 보낼 수 있습니다.

#### 2. 410 Gone 에러가 사라진 이유
- PushSubscriptionManager가 활성화되어 있으면,  
  **만료된 구독(브라우저에 남아있는, 서버 DB에는 ACTIVE로 남아있는 구독)**을  
  클라이언트가 다시 서버에 등록하거나,  
  서버가 여전히 ACTIVE로 인식해서 푸시 발송을 시도할 수 있습니다.
- 이 과정에서 서버가 만료된 구독에 계속 푸시를 시도 → 410 Gone 에러 반복

- **주석 처리 후에는**  
  클라이언트가 더 이상 만료된 구독을 서버에 등록/갱신하지 않으므로  
  서버가 푸시를 시도할 대상이 없어지고,  
  410 Gone 에러가 더 이상 발생하지 않게 됩니다.

#### 3. 만료된 구독을 지우는 동작이 있었는가?
- PushSubscriptionManager가 실제로 **만료된 구독을 서버에서 삭제**하거나  
  **상태를 EXPIRED로 바꾸는 API를 호출**했다면,
  - 서버 DB에서 만료 구독이 정리되어 410 Gone 에러가 줄어들 수 있습니다.
- 하지만,  
  대부분의 PushSubscriptionManager는 **클라이언트에서 구독을 해제/재생성**만 하고,  
  서버 DB에서 직접 삭제하는 것은 서버의 푸시 발송 로직(410 Gone 감지 시 EXPIRED로 마킹/삭제)에서 처리합니다.

#### 4. 결론
- PushSubscriptionManager가 활성화되어 있으면  
  만료된 구독이 서버에 다시 등록되거나,  
  서버가 여전히 ACTIVE로 인식해서 410 Gone 에러가 반복될 수 있습니다.
- 주석 처리하면 클라이언트가 더 이상 구독을 등록하지 않으므로  
  서버가 푸시를 시도할 대상이 없어져 410 Gone 에러가 사라집니다.

---

**요약:**  
PushSubscriptionManager가 활성화되어 있으면 만료된 구독이 서버에 남아있거나 재등록되어  
410 Gone 에러가 반복될 수 있습니다.  
주석 처리하면 더 이상 푸시 대상이 없어져 에러가 사라집니다.

---

## web-push 패키지로 푸시 알림 전송 시 발생할 수 있는 주요 에러 및 처리 방법

### 1. **410 Gone (Subscription Expired/Unsubscribed)**
- **의미:** 구독이 만료되었거나 사용자가 알림을 해제
<!-- @import "[TOC]" {cmd="toc" depthFrom=1 depthTo=6 orderedList=false} -->

<!-- code_chunk_output -->

- [클라이언트가 새로운 알람을 세팅하고 알림이 울리게 되는 전체 과정](#클라이언트가-새로운-알람을-세팅하고-알림이-울리게-되는-전체-과정)
  - [1. 클라이언트에서 알람 설정 요청](#1-클라이언트에서-알람-설정-요청)
  - [2. 서버에서 알람 설정 처리](#2-서버에서-알람-설정-처리)
  - [3. 서버에서 알람 예약](#3-서버에서-알람-예약)
  - [4. 알람 트리거 시점](#4-알람-트리거-시점)
  - [5. 클라이언트 알림 수신](#5-클라이언트-알림-수신)
- [전체 흐름 요약](#전체-흐름-요약)
- [보조 설명](#보조-설명)
- [알림 시간 설정 UI(모달)와 각 계층의 역할](#알림-시간-설정-ui모달와-각-계층의-역할)
  - [1. **모달(알림 설정 UI)의 역할**](#1-모달알림-설정-ui의-역할)
  - [2. **모달의 부모 컴포넌트의 역할**](#2-모달의-부모-컴포넌트의-역할)
  - [3. **서버의 역할**](#3-서버의-역할)
- [전체 흐름 요약](#전체-흐름-요약-1)
  - [코드 설명:](#코드-설명)
    - [의미](#의미)
    - [결론](#결론)
    - [실무적 의미](#실무적-의미)
  - [[문제 분석]](#문제-분석)
  - [[원인]](#원인)
  - [[해결 방법]](#해결-방법)
  - [[요약]](#요약)
  - [410 Gone 에러 원인 분석](#410-gone-에러-원인-분석)
    - [원인](#원인-1)
    - [해결 방법](#해결-방법-1)
- [PushSubscriptionManager 적용 위치 안내](#pushsubscriptionmanager-적용-위치-안내)
  - [1. **PushSubscriptionManager란?**](#1-pushsubscriptionmanager란)
  - [2. **적용 위치(권장)**](#2-적용-위치권장)
    - [(1) **앱의 루트 레이아웃 또는 공통 레이아웃**](#1-앱의-루트-레이아웃-또는-공통-레이아웃)
    - [(2) **알림이 중요한 주요 페이지**](#2-알림이-중요한-주요-페이지)
    - [(3) **사용자 프로필/설정 페이지**](#3-사용자-프로필설정-페이지)
  - [3. **요약**](#3-요약)
  - [VAPID 키와 410 Gone(구독 만료) 에러의 관계](#vapid-키와-410-gone구독-만료-에러의-관계)
    - [410 Gone(만료) 에러의 주요 원인](#410-gone만료-에러의-주요-원인)
    - [VAPID 키가 만료/변경되어야 하는 경우](#vapid-키가-만료변경되어야-하는-경우)
    - [410 Gone과의 관계](#410-gone과의-관계)
  - [VAPID 키 재발급 방법](#vapid-키-재발급-방법)
  - [결론](#결론-1)
- [질문 요약](#질문-요약)
  - [답변](#답변)
    - [1. PushSubscriptionManager의 역할](#1-pushsubscriptionmanager의-역할)
    - [2. 410 Gone 에러가 사라진 이유](#2-410-gone-에러가-사라진-이유)
    - [3. 만료된 구독을 지우는 동작이 있었는가?](#3-만료된-구독을-지우는-동작이-있었는가)
    - [4. 결론](#4-결론)
- [web-push 패키지로 푸시 알림 전송 시 발생할 수 있는 주요 에러 및 처리 방법](#web-push-패키지로-푸시-알림-전송-시-발생할-수-있는-주요-에러-및-처리-방법)
  - [1. **410 Gone (Subscription Expired/Unsubscribed)**](#1-410-gone-subscription-expiredunsubscribed)
  - [2. **404 Not Found (Subscription Not Found)**](#2-404-not-found-subscription-not-found)
  - [3. **403 Forbidden**](#3-403-forbidden)
  - [4. **413 Payload Too Large**](#4-413-payload-too-large)
  - [5. **429 Too Many Requests**](#5-429-too-many-requests)
  - [6. **400 Bad Request**](#6-400-bad-request)
  - [7. **Network Error / Timeout**](#7-network-error--timeout)
  - [8. **500 Internal Server Error**](#8-500-internal-server-error)
  - [**실제 코드 예시 (web-push)**](#실제-코드-예시-web-push)
  - [**요약**](#요약-1)

<!-- /code_chunk_output -->

함
- **처리:** DB에서 해당 구독을 `EXPIRED`로 마킹하거나 삭제

### 2. **404 Not Found (Subscription Not Found)**
- **의미:** FCM 등 푸시 서버에서 해당 구독을 찾을 수 없음 (이미 삭제됨)
- **처리:** DB에서 해당 구독을 `INVALID`로 마킹하거나 삭제

### 3. **403 Forbidden**
- **의미:** VAPID 키 오류, 권한 문제, 도메인 불일치 등
- **처리:** 서버의 VAPID 키, 도메인, 환경변수 설정 확인 필요

### 4. **413 Payload Too Large**
- **의미:** 전송하려는 데이터(페이로드)가 너무 큼
- **처리:** 페이로드 크기를 줄이거나, 알림 메시지 단순화

### 5. **429 Too Many Requests**
- **의미:** 푸시 서버에서 rate limit(요청 제한)에 걸림
- **처리:** 재시도 로직, 백오프(backoff) 적용

### 6. **400 Bad Request**
- **의미:** 구독 객체 형식 오류, 잘못된 파라미터 등
- **처리:** 구독 데이터 유효성 검증, 클라이언트/서버 코드 점검

### 7. **Network Error / Timeout**
- **의미:** 네트워크 장애, FCM 서버 장애 등
- **처리:** 재시도 로직, 장애 감지 및 알림

### 8. **500 Internal Server Error**
- **의미:** FCM 등 푸시 서버 내부 오류
- **처리:** 일시적 장애로 간주, 재시도 또는 로그만 남김

---

### **실제 코드 예시 (web-push)**
```typescript
try {
  await webpush.sendNotification(subscription, payload);
} catch (error: any) {
  if (error.statusCode === 410) {
    // 구독 만료/삭제
  } else if (error.statusCode === 404) {
    // 구독 정보 없음
  } else if (error.statusCode === 403) {
    // VAPID 키/권한 문제
  } else if (error.statusCode === 413) {
    // 페이로드 크기 초과
  } else if (error.statusCode === 429) {
    // 요청 제한
  } else if (error.statusCode === 400) {
    // 잘못된 요청
  } else if (error.statusCode >= 500) {
    // 서버 내부 오류
  } else {
    // 기타 네트워크/알 수 없는 에러
  }
}
```

---

### **요약**
- **410/404:** 구독 상태를 EXPIRED/INVALID로 마킹 또는 삭제
- **403:** VAPID 키/도메인 설정 점검
- **413:** 페이로드 크기 조정
- **429:** 재시도/백오프 적용
- **400:** 구독 데이터 유효성 점검
- **500:** 일시적 장애, 재시도 또는 로그
- **네트워크:** 재시도, 장애 감지

**실제 서비스에서는 각 에러별로 적절한 로깅과 사용자/운영자 알림, 자동 정리 로직이 필요합니다.**
